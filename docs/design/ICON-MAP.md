# Тайга — карта иконок (фича → icons8 «Liquid Glass Color»)

Стиль: icons8 **Liquid Glass Color**. Источник по лицензии Damir: MCP `icons8mcp` (по имени) ИЛИ Downloads-таб аккаунта. Квота 100/мес → этот список = ровно ~100 иконок на ВЕСЬ продукт (1:1), тянуть по разу, кэшировать как SVG-ассет `taiga-web/src/icons/`.

Формат: `фича → icons8-поисковый-термин` (точный slug подтвердится при пуле через MCP/поиск).

## Глобальный шелл (10)
- Поиск ⌘K → `search`
- Память (рейл) → `brain`
- Проекты → `folder`
- Маркет → `shop` / `marketplace`
- Настройки → `settings` / `gear`
- Док-индикатор прогонов → `activity` / `pulse`
- Уведомления → `bell`
- Профиль/аккаунт → `user-circle`
- Тема → `contrast` / `paint-palette`
- Язык RU/EN → `language` / `globe`

## 6 столов (mode-tabs)
- Чат → `chat` / `speech-bubble`
- Дизайн → `design` / `pen-tool`
- Студия → `sparkle` / `magic-wand`
- Агент → `robot` / `bot`
- Код → `source-code` / `code`
- Ultra → `star` / `flash` (особый, мультиколор)

## Разговор (20)
- Без цензуры → `unlock` · Мозг → `brain` · Совет → `group` / `council` · Сравнить → `compare`
- Relay → `relay-race` / `swap` · Дебаты → `debate` / `vs` · Рерайт отказа → `refresh`
- Веб-поиск → `web-search` / `globe-search` · Супер-поиск → `search-plus` · Глубоко → `meditation` / `deep`
- Ресёрч → `research` / `telescope` · Проверка реальности → `verified` / `fact-check`
- Источники правды → `shield-check` / `source` · Подсказки-продолжения → `lightbulb` / `suggestion`
- Модель/каталог → `list` / `catalog` · Персонажи → `theater-masks` / `persona` · Улучшить промпт → `wand`

## Студия (10)
- Картинки → `image` / `picture` · Видео → `video` / `film` · Аватар → `avatar`
- Кино → `clapperboard` / `movie` · Реклама (UGC) → `megaphone` / `ad` · Музыка → `music`
- Озвучка (TTS) → `microphone` / `voice` · 3D → `cube-3d` · Режиссёр → `storyboard` / `director`
- Медиа-инструменты → `tools` / `magic-edit`

## Дизайн (10)
- Прототип → `prototype` / `wireframe` · Слайды → `presentation` · Лайв-правка → `cursor-edit`
- График → `bar-chart` · Таблица → `table` / `spreadsheet` · Диаграмма → `flow-chart` / `diagram`
- 3D-сцена → `3d-rotate` · Веб-артефакт → `code-window` / `browser` · Дизайн-система → `design-system` / `swatches`
- Рисовалка → `brush` / `pencil-draw`

## Агенты (17)
- Сделать агента → `add-bot` · Мои агенты+маркет → `bots` / `shop` · Команда → `team` / `group`
- Оркестратор → `network` / `hierarchy` · Агент-ОС → `dashboard` / `control-panel` · Конвейер → `pipeline` / `workflow`
- Ralph → `loop` / `cycle` · Студия лупов → `repeat-edit` · Галерея лупов → `grid-loop`
- Луп-оценки → `test` / `checklist` · Рутины → `clock-task` / `routine` · Расписание (cron) → `calendar-clock`
- Доска задач → `kanban` / `board` · Прогоны → `history` / `runs` · Оценки → `star-rating`
- Песочница агента → `sandbox` / `flask` · Гайд 3 режима → `help` / `guide`
- **Спенд-кап** → `wallet-limit` / `gauge` · **Док прогонов** → `activity` · **Права/вопрос** → `question` / `shield-question`

## Навыки и код (14)
- Скиллы → `puzzle` / `skill` · Маркет скиллов → `shop` · GitHub-импорт → `github`
- Конструктор команд → `command` / `slash` · Конструктор функций → `function` / `code-block`
- Терминал → `terminal` · Двойной терминал → `split-screen` · Запуск кода → `play-code`
- Песочница моделей → `flask` / `playground` · Браузер → `browser` / `chrome` · Со-пилот экрана → `screen-share`
- Хуки → `webhook` / `hook` · Чат-хуки → `chat-bolt` · MCP-серверы → `plug` / `connector`

## Память и данные (10)
- Память → `brain` · Экспорт памяти → `download` · База знаний (RAG) → `database` / `knowledge`
- Видео-RAG → `video-search` · Проекты → `folders` · Эпизодический поиск → `history-search`
- Источники правды → `shield-check` · Живой документ → `document-sync` · Файлы на сервере → `lock-file` · Рабочая память → `scratchpad` / `notepad`

## Настройки (16)
- Профиль → `user` · Модели → `cpu` / `ai` · Биллинг/кошелёк → `wallet` · Интеграции → `plug`
- Тема/палитра → `paint-palette` · Язык → `language` · Каталог фич → `apps` / `grid`
- Аналитика расхода → `chart-money` · Диагностика → `heartbeat` / `health` · Счётчики подписки → `gauge`
- Библиотека промптов → `bookmarks-prompt` · Закладки → `bookmark` · Что нового → `gift` / `new` · Гид по режимам → `compass`

---
**Итого ~100 иконок = вся система (квота лежит 1:1).** Цвета: каждая иконка из liquid-glass-color уже мультиколор; для consistency держим семьи по группам (Разговор=сине-фиолет, Студия=розово-оранж, Агенты=тил, Код=циан-индиго, Память=индиго, Настройки=нейтрал, Ultra=триколор). Когда подключишь MCP / скачаешь пачкой — тяну по этому списку, по разу, кэширую в `taiga-web/src/icons/`.
