# Дизайн — 2 главных, остальное в _donors/

## ДВА дизайна, которые важны:
1. **НОВЫЙ (строить КАК это) = `design-v3-current-shell-jun17.html`**
   Текущий канон: macOS-окна + convo-dock + macOS-док (уже есть, оценить+почистить, не пересобирать).
   Открывается в браузере, но полностью оживает только в запущенном аппе (:3000 + :8777).
2. **СТАРЫЙ (РЕФЕРЕНС — брать фичи) = `usertest-jun15/`**
   Старый вид с юзер-теста, который Damir любит. 32 экрана-панели + 2 PDF-каталога + 90 flow-скринов.
   Отсюда тянем элементы/фичи в новый — ТОЛЬКО когда Damir укажет что именно.

## Вспомогательное (не «версия дизайна»):
- `design-icons-liquid-glass.html` — набор иконок · `assets/` — 4 концепта лого
- `FEATURE-LEDGER.md` (139 фич) + `GAPS-RECOVERED.md` — реестр фич

## _donors/ — старые промежуточные версии (история, обычно НЕ нужны)
v0 (React-чат 12июн) · v1 (539KB resize-окна 13июн) · v2 (redesign 16июн).
Брать оттуда — редко и только если Damir прямо скажет. Иначе игнор, чтоб не путаться.

**Правило: строим по НОВОМУ (v3), фичи смотрим в СТАРОМ (usertest-jun15). Два дизайна, не путаться.**
