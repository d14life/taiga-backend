# НОВЫЕ МЕХАНИЗМЫ СИСТЕМЫ (что значат + команды)

Дельта к базовому промпту. Главная точка входа — `docs/MANIFEST.md`, читай первым. Это краткая
шпаргалка по всему, что добавлено в систему сборки.

## 1. ФИЧА-ЗА-ФИЧЕЙ + двойной критик на каждый бит (docs/TEST-WORKFLOW.md)
Строй как дом: фундамент → стены → ВНУТРИ слоя одна фича до ИДЕАЛА
(оценка→строй→тест→скрин→КРИТИК→фикс) → потом следующая. Не строить N+1, пока N не безупречна.
Слой принят → следующий слой.

## 2. ПЛАНКА КАЧЕСТВА = docs/ENGINEERING-PRINCIPLES.md
11 frontier-стандартов (как staff топ-компаний): простота/SRP/строгие-типы/все-состояния/
безопасность/перф/a11y/читаемость/тесты/ревью/прод-готовность. Каждый бит судится по своду.
Не «работает» — а безупречно.

## 3. ОЦЕНКА КОДА (не слепо сохранять, не слепо переписывать)
На каждом слое сначала ОЦЕНИ существующий код (работает? чисто? заглушки/мёртвое/раздутое —
chat.tsx 8157, server.py 15125?) → сохрани поведение байт-в-байт → почисти/декомпозируй
(strangler-fig). Док уже есть — тоже оценить+почистить, не пересобирать с нуля.

## 4. ДВОЙНОЙ КРИТИК на КАЖДЫЙ бит (анти-само-суд)
- внутренний агент-критик (Claude, multi-lens, по 11 принципам)
- внешний СОВЕТ (другая модель + веб):
  ```
  python3 tools/external-critique.py --synth-only "идеальна ли фича X?" --file <путь>   # LITE, дёшево, на каждый бит
  python3 tools/external-critique.py --deep --file <путь> "<вопрос>"                     # глубоко (+deepseek), на важное
  ```
  (NVIDIA deepseek+llama + Serper-веб + chairman-синтез; работает из РФ. Gemini — только при рабочем
  выходе сервер/Молдова/Албания, из РФ напрямую заблокирован.)
  Пока ОБА критика не сказали ИДЕАЛЬНО — не «готово».

## 5. ПАМЯТЬ как MCP в аппе — спрашивай вместо грепа/догадок
Лестница дешевизны (docs/EFFICIENT-MEMORY.md) — бери самый дешёвый, что отвечает:
```
graphify path "A" "B"   ·   graphify explain "X"   ·   graphify query "вопрос" --budget 600   # граф КОДА
gbrain search "..."      # ретрив доков (дёшево, без LLM) → синтезируй сам; НЕ gbrain think (лишний LLM)
```
После крупных правок доков/кода: `bash tools/reindex-memory.sh` (graphify + gbrain + чистка _archive).

## 6. АГЕНТ-РЕГИСТРАТОР (ничего не терять)
Каждое решение/просьбу Damir/находку → `docs/DECISIONS-LOG.md` (новое наверх) + `gbrain capture "<суть>"`.

## 7. ФИЧИ = 139 (docs/design/FEATURE-LEDGER.md — главный реестр), не 182
Будущие → docs/PARKED-FEATURES.md. В фазе ПОНИМАНИЕ объедини LEDGER + FEATURE-INVENTORY +
server.py self_manifest() + 32 панели usertest-jun15 + 90 flow + 48 скринов → один чек-лист, не потерять.

## 8. ДВА дизайна (docs/design/README.md)
НОВЫЙ (строить как) = `docs/design/design-v3-current-shell-jun17.html`. СТАРЫЙ (референс фич) =
`docs/design/usertest-jun15/`. v0/v1/v2 в `docs/design/_donors/` — игнор. Не путаться.

## 9. РЕПО ПОЧИЩЕН
63 устаревших дока → `_archive/` (НЕ читать как истину). Канон — в корне (23) + docs/.

## 10. СКОУП + СТАТУС
Security мина #0 (RCE/SSRF/инъекции) УЖЕ сделана ✅ — не переделывать, довынести в authz.py.
Платёж #1 + масштаб (Фаза D) = ОТЛОЖЕНЫ (гейт перед публичным выкатом, не сейчас). Строй всё остальное.

## 11. ЭКОНОМИЯ ТОКЕНОВ
ponytail full (−22%), opusplan (Opus планирует / Sonnet делает дешёвое), параллельные агенты =
изолированный контекст, совет LITE по умолчанию, кап вывода + «отвечай коротко».

## 12. КОГДА СПРАШИВАТЬ Damir
ТОЛЬКО когда реально не уверен после памяти+совета, ИЛИ развилка продукт/прайс/название-фич/деньги.
Технические решения — сам. Мелкими вопросами не сыпь. Решай где можешь (широкая авторизация).
