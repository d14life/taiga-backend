# Парковочные фичи — НЕ ПОТЕРЯТЬ (строить ПОСЛЕ «мокап жив на 100% + тест пройден»)

Damir прямо просил ПОМНИТЬ это и не забыть. Эти фичи отложены НА ПОСЛЕ того, как
основной продукт оживлён на 100% фич и прошёл endless юзер-тест. Это НЕ выкинуто —
это следующая волна. Каждая = задача, ничего не теряется (RULE 0).

## Большие отложенные фичи
1. **Постоянный голосовой режим** — hands-free conversation loop, авто-озвучка ответов,
   непрерывный разговор (не разовый TTS). Studio voice уже есть; это — постоянный режим поверх.
2. **Sandbox-доступ к файлам (общая песочница)** — ключевая идея Damir: ИИ `run_code` И
   терминал юзера бьют в ОДНУ песочницу (E2B, 10ГБ) → общая ФС, общее состояние. Файлы юзера +
   файлы ИИ в одной песочнице, оба видят и используют. Маппинг chat_id → sandbox_id;
   run_code/terminal/files — через эту сессию. Files-вкладка листает ФС песочницы.
3. **Трансформация навыков (skill transformation)** — см. SKILL-TRANSFORMER-PLAN.md.
   Превращение/адаптация скиллов под задачу.
4. **MCP-подключения и их работа** — полноценный MCP-маркетплейс + connect/use потоки
   (база есть: skill-install, MCP-marketplace; докрутить connection + working flows).
5. **Память следующего уровня** — CLaRa / RAG-Anything (CLaRa-7B MLX), гибрид BM25+RRF,
   pgvector+HNSW; g-brain на LangGraph; граф памяти (визуализация); Mem0 за RAG.
6. **БД JSON → Postgres** — миграция при росте.
7. **billing.py YooKassa + идемпотентное зачисление** — мина #1, монетизация (может идти раньше).
8. **Obsidian-интеграция** — опционально, для ручного просмотра доков.
9. **Точные Figma macOS-глиф-иконки** (Monterey/Big Sur/Ventura) — когда снят лимит Figma-MCP;
   пока free icons8-глифы. Damir 2026-06-17: «finish the website, Figma later».

## Правило
Сначала — ОЖИВИТЬ основной продукт на 100% (182 фичи по FEATURE-INVENTORY) + пройти тест.
ПОТОМ — эта волна. Не строить парковочные раньше, если Damir не скажет.
Связано: docs/MANIFEST.md, FEATURE-INVENTORY.md, SKILL-TRANSFORMER-PLAN.md, FUTURE-TASKS.md.
