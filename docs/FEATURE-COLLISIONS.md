# FEATURE COLLISIONS + HARD FEATURES — детальный разбор (для Damir)

Где фичи ПЕРЕСЕКАЮТСЯ/конфликтуют, что УЖЕ построено, и какие фичи технически ТЯЖЁЛЫЕ.
Источник: COMPLETE-FEATURE-MAP (186).

> 🔴 ПРАВИЛО DAMIR (2026-06-19): НЕ удалять/консолидировать НИЧЕГО во время сборки. Строить ВСЁ,
> а дубли удалять В КОНЦЕ (когда всё построено — на случай если что-то окажется важным). Безопаснее.
> Этот документ = ЗАМЕТКИ на потом, НЕ инструкция резать сейчас.
>
> 🔴 ПОПРАВКА: «5 контролов как-сильно-думать» = ОШИБКА Claude. Это ОДИН контрол **Усилие** с 5
> УРОВНЯМИ (Быстрее↔Умнее), НЕ 5 конкурентов. Кластер #7 ниже — невалиден, игнорировать.
>
> ⚠️ ВАЖНО: этот разбор сделан по ДОКЕ, не по живому аппу — Claude несколько раз ошибся (выдумал
> коллизии, переоценил «удалить», переоценил «тяжёлые»). Истина = ЖИВОЙ апп. Ничего не резать по нему.
> Большинство «коллизий» = РАЗНЫЕ фичи на ОДНОМ бэкенде (консолидировать бэкенд, ЭКРАНЫ ОСТАВИТЬ),
> а НЕ «удалить фичи». Реальное удаление ≈ 3-5 настоящих UI-дублей (Дебаты×2, Источники-правды×3) +
> свести 5 контролов глубины к одному. НИЧЕГО не удалять без проверки в ЖИВОМ аппе. «Память ×8» —
> НЕ дубли (vector/видео/эпизодик/знания = разные задачи); лишние только неальт-движки (CLaRa/Mem0/
> Letta/RAG-Anything), которых ещё нет → просто не строить. Не «режем 25-30», а консолидируем бэкенды.

## КЛАСТЕРЫ КОЛЛИЗИЙ (дубли — консолидировать, не строить все)

### 1. ПАМЯТЬ / RAG ×8 🔴 (самый большой)
vector-RAG(✅) · Mem0/Letta(✅) · graphify-в-продукте · RAG-Anything/CLaRa(🟡) · видео-RAG(✅) ·
эпизодик-recall(✅) · База знаний(✅) · рабочая-память(⬜) · живой-документ(⬜).
- **Уже построено:** ядро RAG (BM25+вектор) + recall + база знаний + видео-RAG — ЖИВЫ.
- **Коллизия:** 8 систем «помнить» делают одно и то же разными движками.
- **Решение:** оставить 1 (живой RAG) + опц. граф-память. Остальное (CLaRa/RAG-Anything/живой-док/
  рабочая-память) — НЕ строить. Экономит ~6 фич + кучу инфры.

### 2. ВЫБОР МОДЕЛИ ×6 🟡
Модель/каталог-чат(🟡) · топбар-modelpick(mock) · Модели-настройки(✅) · Песочница-моделей(🟡) ·
композер-pill «Авто»(✅) · Живой-каталог(✅).
- **Уже построено:** настройки Модели + композер-pill + живой-каталог — ЖИВЫ. Топбар-pick = mock.
- **Коллизия:** 6 мест выбрать модель → какой источник истины? рассинхрон.
- **Решение:** ОДИН бэкенд-каталог, показать в 2 местах (композер-pill + настройки). Остальные 4 — алиасы.

### 3. МАРКЕТЫ ×6 🟡
Маркет скиллов(✅) · Маркет агентов+скиллов(✅) · Все скиллы(✅) · Библиотека 300+/3000+(🟡) ·
Магазин кнопок app-store(✅) · Мои агенты+маркет(✅).
- **Уже построено:** почти все ЖИВЫ (разные UI на пересекающийся install-флоу).
- **Коллизия:** скилл-маркет vs агент-маркет vs кнопка-стор — дублирующая установка-по-ссылке.
- **Решение:** ОДИН стор с вкладками (скиллы/агенты/кнопки). Бэк один.

### 4. ПЕСОЧНИЦА/ИСПОЛНЕНИЕ ×6 🟡
Песочница-агента-E2B(🟡) · Песочница-моделей(🟡) · E2B/Daytona/Modal(🟡) · Запуск-кода(✅) ·
Терминал-E2B(✅) · Конструктор-функций-sandbox(✅) · Чат-хуки-sandbox(🟡).
- **Уже построено:** терминал + запуск кода + функции-sandbox — ЖИВЫ.
- **Коллизия:** сколько E2B-контейнеров на юзера? дорого (10ГБ/песочница), сложно.
- **Решение:** ОДНА общая песочница на чат (идея уже в логе). run_code+терминал+файлы — в неё. Не плодить.

### 5. ПОИСК ×7 🟢 (не баг, но перегруз)
Поиск-по-чатам(✅) · overlay-поиск(✅) · ⌘K-Feature-Map(✅) · File-Search(🟡) · Супер-поиск(✅) ·
Веб-поиск(✅) · эпизодик-recall(✅).
- **Уже построено:** почти все ЖИВЫ (разные скоупы: чаты/фичи/файлы/веб/память).
- **Коллизия:** не дубль (скоупы разные), но 7 точек входа = UX-перегруз.
- **Решение:** оставить, но ⌘K = единая точка, роутит по скоупам.

### 6. ДЕНЬГИ ×6 🟡
Биллинг/кошелёк(🟡) · Пополнение(🟡) · Balance(✅) · Счётчики-подписки(⬜) · Аналитика-расхода(⬜) ·
Цена-перед-ген(✅).
- **Уже построено:** баланс + цена-перед-ген ЖИВЫ. Биллинг/пополнение 🟡. Счётчики/аналитика ⬜.
- **Решение:** ядро = баланс+пополнение+цена (почти готово). Счётчики/аналитика — бэклог.

### 7. ГЛУБИНА/УСИЛИЕ ×5 🟡 (путает юзера)
Глубоко(🟡) · Усилие-слайдер(✅) · Два-диска-глубина-vs-длина(🟡) · Нативный-reasoning_effort(🟡) ·
Авто-уровень-сложности(⬜).
- **Уже построено:** Усилие-слайдер ЖИВ. Остальные 🟡/⬜.
- **Коллизия:** 5 контролов «как сильно думать» = юзер запутается (слайдер vs мод Глубоко vs диск).
- **Решение:** ОДИН параметр Усилие (слайдер), он гонит нативный reasoning + подбор модели. «Глубоко» = пресет усилия.

### 8. ДУБЛИ-ОДИНОЧКИ
- **Дебаты ×2:** в Чат-столе И в Агент-столе (один `/api/debate`). → один UI/алиас.
- **Источники правды ×3:** Чат + Память + Агент-саб-таб (один `/api/sources`). → показать один раз.
- **Оркестрация ×4:** Оркестратор + Команда + Конвейер + Агент-ОС — пересекаются. → консолидировать.

## «ТЯЖЁЛЫЕ» ФИЧИ — поправка Damir (большинство РЕШЕНО донорами/автоматизацией, НЕ from-scratch)
| Фича | Построено | Решение Damir (не так тяжело) |
|---|---|---|
| **Видео / 3D-ген** | 🟡 | ИНТЕГРАЦИЯ из `open-generative` (делает медиа) + спец-3D-модель + новейшая голос-модель. Не с нуля. |
| **Общая E2B-песочница** | 🟡 | НЕ хранить у нас: юзер ПЛАТИТ за storage (как Higgsfield) ИЛИ остаётся в браузере ИЛИ ДОСТУП к файлам юзера без хранения. Cost решён. |
| **Биллинг+оплата** | 🟡 | Единственный реальный риск (RU-ToS uncensored↔платёж) — бизнес, не код. Крипта/зарубеж. |
| **~800 моделей каталог** | ✅ | АВТО-CRON-JOB проверки + фантом-детект (jobs-инфра уже есть). Не «вечная ручная поддержка». |
| **Голос hands-free** | ✅баз | Решено — Instagram-reel умеет, Gemini подтвердил, blueprint есть. Не тяжело. |
| **Браузер-агент/со-пилот** | ✅ | Уже live. |
| **Ultra пересборка-разговором** | ⬜ | ПОРТ из `open-design`+`open-generation`+`open-studio` (делают UI-ген на base-модели как Claude Code). Не с нуля. |
| **JSON/SQLite → Postgres** | ⬜ | Нужно СДЕЛАТЬ в какой-то момент (Damir: обязательно, не блокер сейчас). |

ВЫВОД (Damir прав): почти все «тяжёлые» = ИНТЕГРАЦИЯ из донор-репо (open-generative/open-design/
open-studio) + автоматизация (cron) + арх-решения (не хранить файлы). НЕ from-scratch-тяжесть.
Единственный настоящий риск — биллинг при uncensored (бизнес, не код). Я перепессимил по доке.

## ИТОГ
- **8 кластеров-коллизий** → консолидация режет **~25-30 дублирующих фич** (большинство ядра в них УЖЕ построено как 1 рабочая система, остальные — лишние UI/движки).
- **8 тяжёлых фич** — почти все НЕ в MVP-ядре (видео/3D/Ultra-пересборка/Postgres → бэклог; биллинг — ядро но бизнес-риск).
- **Что строить из коллизий:** в каждом кластере оставить ОДНУ рабочую (она обычно уже live), остальные алиасить/выкинуть.
