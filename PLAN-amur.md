# PLAN — amur (STUDIO)

Порядок по принципу «заплатил БЫ из-за этого?» — глубина, не ширина. Никаких заглушек.

## T1 — Кино MP4 экспорт: довести видео-ветку до конца ⏳
Image-склейка работает. Проверить видео+картинка (скачать https + нормализовать + concat).
Если падает на codec/скачивании — починить. Это headline-отличие (один скачиваемый фильм).
DONE = смешанный фильм качается как валидный MP4 (e2e).

## T2 — Кино «Режиссёр»: одна идея → авто-раскадровка ⭐ pay-feature
Кнопка «Сценарий по идее»: юзер пишет одну строку → текст-модель (через существующий /api/chat,
без правки файлов Core) возвращает 3-6 сцен (тип+промпт+камера) → заполняет раскадровку.
Это «plug in the agent» которое просил владелец: «напиши идею — получи фильм». Чистый фронт.
DONE = ввёл идею → раскадровка заполнилась осмысленными сценами → «Снять всё» работает.

## T3 — Доп. фото-тулзы через Venice (фон/outpaint/стиль)
Проверить что умеет Venice edit: фон-ремув, расширение кадра (outpaint), смена стиля.
Добавить рабочие в PhotoTools (только то что реально выдаёт результат). Без заглушек.
DONE = каждый добавленный инструмент даёт реальный результат (curl-проверен).

## T4 — Студия: онбординг + устойчивость
Пустые состояния с примерами-кнопками, ретрай-на-ошибке генерации, понятные ошибки,
проверка мобильной раскладки студии. «Raw → готовый продукт».
DONE = пустая студия объясняет себя, ошибки понятны, ретрай есть.

## T5 — Плейграунд per-model
На модель: оценка цены до генерации (есть), пример-промпт, JSON-параметры (seed/steps/guidance),
кнопка «copy as API call». DONE = можно тонко настроить и скопировать вызов.

## Заблокировано (не мой провайдер) — в QUESTIONS
- Музыка (Suno) и 3D: NanoGPT не умеет. Нужен др. провайдер — решение владельца.
- NanoGPT-картинки в каталог: ingestion = зона Core/BACKEND (попросить через доску).

Текущий: T1 (тест в фоне) → сразу T2.
