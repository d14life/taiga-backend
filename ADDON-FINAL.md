# ADDON-FINAL — вставь во ВСЕ 4 сессии (каждая берёт ТОЛЬКО свою лану). Заменяет прежние add-on.

Damir сказал «do all» — доводим ВСЁ. Прочитай FINISH-PLAN.md (разделы «НАЙДЕНО ПРИ СВЕРКЕ»,
«SELF-KNOWLEDGE», «БЕСПЛАТНЫЙ скилл-рендер», DoD). После текущей задачи бери по порядку СВОИ:

## CORE (kedr) — chat.tsx surface
1. 🔴 **P0-СБРОС студии** — подтверди/почини именно РЕМАУНТ студии (не путать с фоном-радугой,
   тот уже починен). amur флагает что P0 ещё открыт. Устойчивый mount/state студии.
2. **/pipeline — юзер-конструктор цепочек модель→модель→модель** (ГЛАВНАЯ фишка). sendPipeline в
   бэке есть → нужен UI-билдер (/new + /pipeline).
3. **3 яруса Обычный/Студия/Ультра** — реструктур верх-навигации (3 входа). Fast/Auto/Expert/Heavy
   остаётся осью усилия ВНУТРИ яруса. Ультра = топ-модель + видео/картинки внутри + no-refusal.
4. **/uncensor команда** (фронт, зовёт rewrite_uncensored от ural).
5. **Тоггл-лимит трат на операцию** («стоп после $X / экстра-макс»).
6. Доделать **«+»-tools-меню** (File Search/Run Code/Skills/Artifacts).
7. **Хук тем/Voice** — дай sosna точку инъекции (CSS-переменные тем + mic-STT), чтоб разблокировать её.

## STUDIO (amur) — медиа
1. **БЕСПЛАТНЫЙ скилл-рендер — КАРТИНКИ+ВИДЕО кодом** (паттерн prajwal-y/video_explainer): LLM пишет
   сцены (Remotion/manim) → FFmpeg рендерит MP4 → объяснялки/шортсы/слайды/дата-виз БЕЗ платной модели.
   Озвучка Edge-TTS (free), фон MusicGen. Картинки: диаграммы/постеры/SVG (canvas/p5). Кирпичи: remotion/manim-video/canvas-design.
2. **Видео по референсу** (вход референс-видео → стиль + свой текст).
3. 3D включить, когда AIMLAPI triposr поднимется (одна строка).

## BACKEND (ural) — server.py ядро
1. **rewrite_uncensored(prompt)** — переписчик под /uncensor (любая модель даёт uncensored-эквивалент).
2. **Генерация-роутер**: классификатор «запрос → free-код-рендер vs платная модель» (диаграмма/объяснялка/шортс → free; фотореал/кинематограф/Suno/3D → модель).
3. **Auxiliary-модели на под-задачу** (Vision/Web-extract/Compression/Title — дешёвая, дефолт «main»).
4. **self_manifest()** — Тайга знает себя (интроспекция реестров → инжект кратко в систему).
5. **MCP-коннектор при создании скилла/агента** + помоги фронту подключить MCP-UI (твой контракт).
6. **T9/опечатки + семантика-с-контекстом** (инструкция в систему + опц. нормализация).

## CORE-B (sosna) — библиотеки/вид
1. Скилл **«Сленг»** (если ещё нет).
2. **Темы/палитры** + **Voice STT** — через хук от kedr (см. CORE#7); если хук не готов — пинг координатору, бери Bookmarks/Memories-панель пока.
3. **Bookmarks** (закладки сообщений) + **Memories-панель** (редактор памяти-на-чат).

## ВСЕ
Замок перед чужим/общим файлом · tsc/ast после каждой правки · проверка вживую · бинарный STATUS ·
НЕ трогай ⛔отложенное (оплата · гард-дипфейк · соц-публикация · биллинг-модалка). Координатор гейтит финиш.
