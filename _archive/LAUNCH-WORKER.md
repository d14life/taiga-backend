# Запуск рабочей сессии Тайги (копипаст в новую сессию Claude)

**Где открыть:** в приложении Claude Code → новая сессия → рабочая папка
`/Users/damir12/Downloads/claude-sessions/2026-06-10/mostik-ai` → вставить промпт ниже → Enter.

**Параллельно:** открой 2–3 новые сессии тем же способом, в каждой поменяй `session_id`
(kedr2 / amur2 / ural2). Замки `.agent-locks/` не дадут им затереть друг друга.

**Эту сессию НЕ трогай** — она координатор (25-мин луп: читает STATUS/замки, шлёт отчёт).

---

## ПРОМПТ (копировать целиком)

Ты — рабочая сессия флота Тайги. session_id: **kedr2** (если открываешь вторую/третью — поменяй на amur2 / ural2). Корень: `/Users/damir12/Downloads/claude-sessions/2026-06-10/mostik-ai`. Фронт: `taiga-web` (Next.js 16 / React 19 / Tailwind 4). Бэк: `server.py`. Приложение живёт на http://localhost:3000/app.

СНАЧАЛА прочитай (в этом порядке): `FINISH-PLAN.md` (Definition of Done + распределение лан S1–S5), `FINISH-ALL.md` (что уже сделано `[x]`), `AGENTS-BOARD.md` (протокол замков `.agent-locks/`), и в `../taiga-redesign/`: `IA-REFACTOR-PLAN.md` + `ACCEPTANCE-TEST-PLAN.md`. Загляни в свежие `STATUS-*.md` (последние правки — claude-overnight, Jun 14) чтобы не переделывать готовое.

ШАГ 0 — СКИЛЛЫ: первым делом запусти мета-скилл `pick-skills` (он сам прогонит задачу через нужный стек: процесс → дизайн → react/next-best-practices → верификация). Планка всегда: production-grade, БЕЗ emoji (только line/SVG-иконки), без заглушек/TODO, каждое «сделано» = пруф (tsc + живой скрин/curl). Любая UI-работа обязательно тянет дизайн-скилл (high-end-visual-design / refero-design / design-taste-frontend), код — react-best-practices + next-best-practices.

ТВОЯ ЗАДАЧА — грызть остаток до финиша, НОН-СТОП, без вопросов Damir:

**ЭТАЛОН (визуальный спек, КРИТИЧНО — не повтори прошлую ошибку):** живой `/app` СЕЙЧАС выглядит СТАРО (перегруженный композер на ~40 пилюль, дубли Мозг/Совет/ресёрч/Глубоко) — это НЕ утверждённый дизайн. Эталон = макет `http://localhost:8901/index.html` (исходник `../taiga-redesign/index.html`). Приведи живой апп К ЭТОМУ макету: спокойный центр «Что создаём?» + чипы моделей, верх — 6 столов (Чат·Дизайн·Студия·Агент·Код·Ultra), СЛИМ-композер (одна строка: `+` · Авто · Усилие · микрофон · отправить), 4 подсказки снизу (Собери совет · Глубокий ресёрч · Режим «Мозг» · Дебаты). Перекраска токенов — НЕ редизайн; нужна СТРУКТУРНАЯ трансформация. Каждый перенесённый контрол остаётся доступен (в `[+]`-меню / model-control / Настройки), поведение НЕ удаляем — только переносим, где рендерится.

**A. Доделай IA-рефактор** (`IA-REFACTOR-PLAN.md` — beam и Relay уже убраны, осталось):
1. Слить мульти-модель в один enum `multiModel: null|'council'|'compare'|'brain'|'debate'` (стейты brainOn/councilMembers/compareModels → один контроль; payload-контракт с бэком сохранить).
2. Дебаты (Архитектор↔Критик) — как вариант мульти-модели, дать вход в UI рядом с Совет/Сравнить.
3. Глубина мысли → ОДИН Depth-слайдер; убрать старый 4-кнопочный Усилие-селектор и Deep-пилюлю (дубли).
4. Навигация — 6 столов, единый вход (Чат·Студия·Дизайн·Агент·Код·Ultra); mode-пилюли → дефолты стола; Без-цензуры = тумблер.
5. Расстановка фич: pipeline доступен из Чат/Код, экспорт памяти → Настройки, видео-пилюля → ведёт в Студию>Видео, убрать дубли входов Дизайн/Студия.
Это хирургия по `chat.tsx` (взаимозависимый стейт) — ВОЗЬМИ ЗАМОК на chat.tsx, по ОДНОМУ изменению, `tsc=0` после каждого, НЕ трогай routing/circuit-breaker стейт (logicalModelKey/modelCopies/healthV/fundedModels/providerFunded). Каждый шаг = коммит.

**B. Когда IA зелёный — прогони acceptance-тест** (`ACCEPTANCE-TEST-PLAN.md`): реальные юзер-флоу по КАЖДОМУ столу через ЖИВОЙ апп (Playwright на localhost:3000/app), скриншот каждого шага, сверка с эталоном (как должно выглядеть И как вести себя), РЕАЛЬНАЯ генерация (можно тратить кредиты Damir — NanoGPT/Chutes есть), проверка авто-фолбэка провайдеров (спровоцируй пустой Venice → должно переключиться), аудит ментальной модели на каждом экране («понял бы новый юзер за 3 сек? нет ли двух кнопок про одно?»). Баги → `BUGS-FOUND.md` (экран · шаг · ожидалось · получилось · скрин · severity) → чини волнами, не просто фиксируй.

**C. ЗНАНИЕ ДЛЯ БУДУЩЕГО** (чтоб следующие сессии и Damir масштабировали проект без раскопок) — собери/обнови ИЗ РЕАЛЬНОГО кода два durable-дока в корне:
- `ARCHITECTURE.md` — карта системы: server.py (роутинг / circuit-breaker / балансо-фолбэк / биллинг / RAG / агенты / студия), taiga-web (chat.tsx-ядро, компоненты, lib, select.ts-каталог), orchestrator.py / browser_hub.py / guard.py; ключевые инварианты (НЕ трогать routing/circuit-breaker стейт; защита выбора модели при деградации бюджета; флагман = Xiaomi AX3000T, Lite = Cudy).
- `DEVELOPING.md` — рецепты «как добавить и масштабировать»: новый провайдер/модель, студийная модальность, скилл, агент-воркер, MCP-коннектор, команда, под-таб; как запустить локально (launch.json), как тестировать (tsc / ast / smoke / Playwright), как перезапустить бэк (launchctl kickstart), как работает флот/замки.
Генери из кода, НЕ из памяти — чтоб не устарело. Бэк уже авто-генерит `self_manifest()` — сошлись на него, не дублируй вручную.

**D. МАГАЗИН ФИЧ (App Store — запрос Damir, НЕ забыть):** встроенный браузабельный маркет ВСЕХ возможностей Тайги — режимы, скиллы, агенты, инструменты, MCP-коннекторы, студия-модальности, команды, пайплайны — ВКЛЮЧАЯ те, что нигде в UI не вынесены, чтобы юзер их находил и запускал («many features»). Питать от живого `self_manifest()` / реестров (авто-обновление, не устаревает, не хардкод). Категории + поиск + вкл/запуск каждой, включая скрытые. Расширь существующий зачаток `src/components/tool-marketplace.tsx` (сейчас только MCP/скиллы) до полного стора. Вход — отдельный стол или пункт навигации. Строй ПОСЛЕ приведения поверхности /app к макету (не конфликтуй с активной интеграцией chat.tsx). DoD: открыл стор → все фичи по категориям → можешь включить/запустить любую, включая скрытые.

ПРАВИЛА:
- Не останавливайся спрашивать. Безопасные дефолты. Решения по деньгам/процессору — пропусти (Damir отложил).
- Замок `.agent-locks/` перед правкой chat.tsx / server.py / select.ts (см. `claim()` в AGENTS-BOARD.md). BUSY → возьми другую задачу из плана.
- После КАЖДОЙ правки: из `taiga-web` → `npx tsc --noEmit` (exit 0). Если трогал бэк → `python3 -c "import ast;ast.parse(open('server.py').read())"`. Красное — не двигайся дальше.
- Каждую фичу проверяй ВЖИВУЮ (Playwright/preview/curl/скриншот), не на глаз. «Сделано» = доказано пруфом.
- Heartbeat в `STATUS-<session_id>.md` раз в ~15 мин, бинарные строки.
- Коммить только зелёное (сначала taiga-web, потом корень) + push. Конец сообщения коммита:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- НЕ трогай `amnezia_for_awg`, не вводи пароли, оплату не делай.

ФИНИШ: когда все 🅜 must в Definition of Done зелёные, acceptance-тест пройден с пруфами, И `ARCHITECTURE.md` + `DEVELOPING.md` актуальны — СТОП, отчёт «v1 done» в `STATUS-<session_id>.md`.

Начинай прямо сейчас: прочитай доки → возьми замок на chat.tsx → первый шаг IA (слить multiModel в один enum). Поехали, не тормози.
