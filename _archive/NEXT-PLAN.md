# ГРАНД-ПЛАН СОВЕТА (2026-06-12, после аудита 4 сессий)

Решения Damir: **оплату ОТКЛАДЫВАЕМ** · **AIMLAPI (музыка+3D) — ДА**.
Принцип: хватит наваливать фичи — продукт ~80% готов движком, тонок витриной.
Фокус = разблокировать УЖЕ построенное + добить медиа. Каждая сессия берёт СВОЙ блок ниже.

Координация: перед правкой файла ВНЕ своей ланы — замок `.agent-locks/` (см. AGENTS-BOARD.md).
После каждой правки: `npx tsc --noEmit` (фронт) / `python3 -c "import ast;ast.parse(open('server.py').read())"` (бэк).

---

## 🔴 P0 — для CORE-сессии (kedr): ПОЧИНИ СБРОС СТУДИИ

САМЫЙ ВАЖНЫЙ фикс. Студия (Кино/Видео/Агенты/Тулзы) ПОСТРОЕНА, но **недостижима**: при
ре-рендере chat.tsx студия слетает обратно в «Картинки». Это подтверждённый реальный баг
(не синтетик-клик) — amur нашёл корень: студийный компонент ремаунтится на каждый рендер
родителя → состояние режима сбрасывается.

ЗАДАЧА:
1. Стабилизируй студию: вынеси активный режим студии в устойчивый state (не сбрасываемый
   ре-рендером), дай студийному компоненту стабильный key, оберни в React.memo если нужно.
   Цель: клик Кино/Видео/Агенты ОСТАЁТСЯ, не прыгает в «Картинки».
2. Проверь в браузере: открой Студию → ткни каждый таб → должен держаться. Скриншот/eval.
ВАЖНО: chat.tsx ТЯЖЕЛО правила backend/routing-сессия (кросс-провайдер-роутер,
circuit-breaker `healthyOnly/fundedModels`, балансо-кап). ВОЗЬМИ ЗАМОК на chat.tsx, подтяни
свежак, НЕ затри эти правки (logicalModelKey/modelCopies/healthV/providerFunded остаются).

---

## 🎬 для STUDIO-сессии (amur): МУЗЫКА + 3D через AIMLAPI (Damir дал go)

Ключ уже на месте: `~/.aimlapi_key`. Твоя зона: студийные либы + ген-функции server.py
(api_video/api_audio/api_image/charge_media), НЕ chat.tsx (там kedr чинит P0).

ЗАДАЧА (по порядку):
1. СНАЧАЛА e2e-проверь УЖЕ вшитое AIMLAPI-ВИДЕО через живой UI (код готов, но после
   передачи ключа полный прогон через приложение не подтверждён): сгенери реальный клип из
   UI студии → MP4 приходит + биллинг (charge_media с наценкой) списался. Скриншот.
2. МУЗЫКА: подключи AIMLAPI MiniMax-музыку (~$0.039/трек) — пикер модели + генерация +
   charge_media. Вкладка «Музыка» из «скоро» → рабочая.
3. 3D: подключи AIMLAPI 3D (Hunyuan3D/TripoSR-класс) — генерация + биллинг. Вкладка «3D» → рабочая.
4. Балансо-осознанно: медиа НЕ слать на пустой провайдер (как чат). Ошибки — понятные
   (баланс/провайдер/лимит), не «не удалось».
5. Taste-learning (из gstack, опц. если успеешь): лайк/дизлайк генераций → смещать будущие.

Статус давай бинарно: «AIMLAPI-видео-e2e done / музыка done / 3D done / баланс-роутинг done».

---

## ⚙️ для BACKEND-сессии (ural/atlas) + routing-сессии: ДВИЖОК-ПОДДЕРЖКА

Оплату НЕ трогаем (Damir отложил). Login-бэкенд (auth.py) готов, UI отложен (был payment-gated).
ЗАДАЧА:
1. Поддержи студию: убедись что api_video/api_image/api_audio роутят БАЛАНСО-ОСОЗНАННО
   (пустой провайдер — мимо), как сделано для чата (chat_completions_url/cap_nano_max_tokens).
2. AIMLAPI: проверь routing+billing+poll стабильны под нагрузкой; ключ читается.
3. Circuit-breaker/кросс-провайдер-роутер/балансо-кап (уже сделаны routing-сессией) — держи зелёным.
4. Health-watch: tsc=0, server.py валиден, /api/init 200. Smoke по медиа после правок amur.

---

## ОТЛОЖЕНО (по решению Damir)
- Реальная оплата (процессор + чекаут) — позже, после теста.
- Экран входа (UI) — auth.py готов, ждёт (был привязан к запуску/оплате).
- 3-тира (Normal/Studio/Ultra), темы — низкий приоритет.
