# DISPATCH — вставь ЭТОТ блок в КАЖДУЮ из 4 рабочих Claude Code сессий

Ты — одна из 4 параллельных сессий, доводящих Тайгу до финиша. Координатор (5-я сессия)
следит за доской и гейтит финиш. Действуй САМ, не спрашивай — нон-стоп до конца своей ланы.

## ШАГ 1 — пойми контекст (прочитай, не правь)
- `FINISH-PLAN.md` — план до финиша: блоки S1–S4 + Definition of Done.
- `PARITY-MATRIX.md` — зачем (что у эталонов vs у нас, со 78 скринов Damir).
- `AGENTS-BOARD.md` — ланы + протокол замка `.agent-locks/`.
- `STATUS-*.md` — кто что уже сделал.

## ШАГ 2 — займи ОДНУ лану (через замок, чтоб не столкнуться)
Если ты УЖЕ владел ланой (сделал в ней большую часть) — продолжай ЕЁ. Иначе возьми любую
СВОБОДНУЮ из таблицы (проверь `.agent-locks/` и строки STATUS — какая без свежего heartbeat):

| Блок | Лана | Зона (твои файлы) | Главное |
|---|---|---|---|
| **S1** | CORE-A (kedr) | chat.tsx · use-taiga-chat.ts · model-picker · команды | 🔴P0-фикс студии → «+»-tools-меню (ChatGPT) → режимы Fast/Auto/Expert/Heavy (Grok) → слайдер «память N сообщений» → баг фон-радуга |
| **S2** | STUDIO (amur) | *-gen.ts · cinema/image-studio · ген-функции server.py | музыка + 3D (AIMLAPI оплачен) + e2e-видео → retry «не вышло» |
| **S3** | BACKEND (ural) | server.py ядро · orchestrator/browser_hub/guard | MCP-маркетплейс коннекторов → auxiliary-модели на под-задачу → баланс-роутинг медиа → оркестратор-таймаут |
| **S4** | CORE-B (libraries) | settings-panel · skill-builder · НОВЫЕ панели (НЕ chat.tsx core) | skills-библиотека с тумблерами (Hermes) → темы/палитры + Voice STT → Prompts/Bookmarks/Memories-панель |

Застолби: `claim()` из AGENTS-BOARD.md свою лану в `.agent-locks/`, впиши строку в доску +
заведи/обнови `STATUS-<твой-id>.md`. Если лана занята свежим heartbeat — бери другую.

## ШАГ 3 — грызи нон-стоп (правила)
1. НЕ останавливайся спрашивать Damir. Дефолты. Решения по деньгам/оплате — пропусти (отложено).
2. Замок на файл ВНЕ своей ланы / общий (chat.tsx, select.ts, server.py). BUSY → делай другое.
3. После КАЖДОЙ правки: `npx tsc --noEmit` (фронт, exit 0) / `python3 -c "import ast;ast.parse(open('server.py').read())"` (бэк). Красный → не двигайся.
4. Каждую фичу ПРОВЕРЯЙ вживую (preview_eval/curl/скрин). «Сделано» = доказано, не на глаз.
5. Heartbeat в STATUS раз в ~15 мин. Закончил блок → бери 🅝nice / из ОБЩЕГО БЕКЛОГА (FINISH-PLAN).
6. НЕ трогай ⛔отложенное (оплата, гард-дипфейк, соц-публикация, биллинг-модалка).

## ШАГ 4 — финиш
Когда твой блок зелёный по DoD — отпиши в STATUS «<блок> DONE» и возьми следующий приоритет.
Координатор сведёт, прогонит smoke по всем флоу и даст общий СТОП «v1 done».

Старт: прочитай 4 файла, застолби лану, скажи бинарно что берёшь — и поехал.
