# МАСТЕР-АУДИТ + ПЛАН (5 аудиторов, честно по-юзерски, 2026-06-12)

## ГЛАВНЫЙ ВЫВОД: продукт ЗДОРОВЕЕ чем казалось.
«Сломанность» которую чувствовал Damir = в основном ОДИН файл-прокси (уже починен) + горстка точечных багов. Большинство работает вживую.

## ✅ ЧТО РАБОТАЕТ (подтверждено curl + трассировкой)
- **Чат-ядро:** отправка/стрим/стоп/повтор/правка/копир/форк · модель «прилипает» · авто-роутер · Fast/Auto/Expert · цена-под-ответом · читать-вслух (FREE TTS) · окно-памяти · бюджет · восстановление чата · сайдбар.
- **Экосистема (сильнейший домен):** агенты (маркет+билдер+панель+install-by-link+SSRF) · навыки (маркет+install) · кастом-функции (8 примитивов + AI-сборка) · MCP (полный CRUD). Всё смонтировано+работает.
- **Память:** tombstone-удаление держится · профиль · сленг/стиль · авто-память.
- **Студия:** картинки (free+paid) · видео (AIMLAPI funded) · музыка · 3D-состояние · cinema-export (ffmpeg) · free-TTS.
- **Настройки:** BYOK ✅ · Мозг-конфиг ✅ · темы · голос · бэкап · API-ключи.

## ✅ ИСПРАВЛЕНО ЭТОЙ СЕССИЕЙ
1. **🎯 ПРОКСИ — корень всего** (`app/api/chat/route.ts`): пропускал 6 параметров, РЕЗАЛ 12 (`compare/compareModels/councilModels/depth/sources/mode/temperature/dev/driver/crafter/responder/max_spend`). Фронт слал правильно, прокси резал → «ничего не произошло». **Починено + проверено: сравнение стримит member_answer через :3000.** Разом ожили: Сравнение · Совет-модели · Ресёрч-глубина · Мозг-ведущий · температура · spend-cap · dev-mode · mode.
2. **🏭 ГЕНЕРАТОР ВСЕГО** — Тайга теперь делает файлы/артефакты, рендер живьём + скачать: **PDF · Word · Markdown · Excel/CSV · графики · диаграммы(mermaid) · сайты(превью) · 3D(three.js) · анимация.** Модель научена их выдавать. Прод-сборка PASS.
3. read-aloud → бесплатный Google TTS (без NanoGPT).

## 🔴 ОСТАВШИЕСЯ БАГИ → ПЛАН ФИКСОВ (по волнам)
**Волна A — бэкенд (server.py, 1 воркер):**
- 🔴 **Зрение сломано** — загрузка картинки → `gemma-4-uncensored` (не-vision) → API 400. Роутить на vision_ok модель (`route_model` + гард 6440/6480).
- 🟡 видео-дефолт `wan-video-22-turbo` = пустой NanoGPT → 402. Дефолт → funded `aiml:` + balance-aware фильтр пустых.
- 🟡 `/api/td3` висит 30с (латентно, 3D выключен) — короткий таймаут.

**Волна B — фронт chat.tsx (1 воркер):**
- 🟡 кастом-функция «Веб» мертва в диспатче (`byBase` без `web`) → добавить `web:{agentOn:true}`.
- 🔴 dev-mode per-режим: схема FE `{chat:{}}` vs бэк `{modes:{chat:{}}}` → конфиг дропается + само-стирается при открытии. Чинить схему.
- 🔴 dev-mode «безопасные тулзы» тумблеры мёртвы (диспатч не читает `.tools`).
- 🔴 FunctionBar пиллы Терминал/Запуск/Дифф/Воркер/**Видео** — все = agentOn, не различны (Видео ≠ студия). Развести или убрать.
- 🔴 5 слэш-заглушек `/projects /vision /video /agents(дубль) /checkpoint` — тосты «скоро». Развести на реальное или убрать.
- 🔴 privacy-радио косметика (нет эффекта) — либо включить серверную синхронизацию, либо скрыть.

**Волна C — студия (1 воркер):** Студия-Аудио вкладка → free-TTS вместо платного 402.

**Отложено (Damir):** пополнение/оплата (BTCPay когда расцензурим).

## ИТОГ
Корень починен, генератор-всего собран. Осталось ~10 точечных багов (Волны A/B/C) — раскидать по 3 воркерам, и **v1 = реально готов к юзерам.**
