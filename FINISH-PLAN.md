# ТАЙГА — МАСТЕР-ПЛАН ДО ФИНИША (5 сессий × ~20ч нон-стоп, Opus 4.8 1M)

Цель: довести Тайгу до **v1 «как фронтир, но наш + uncensored + крипта»** и ОСТАНОВИТЬСЯ.
Список конечный (см. Definition of Done). Фичи сверх него НЕ берём — финишируем.

Эталоны (скрины Damir): ChatGPT (File Search/Web/Skills/Run Code/Artifacts),
LibreChat (Agent Builder/Prompts/Memories/MCP-коннекторы/Bookmarks), Grok (Fast/Auto/Expert/Heavy).
AIMLAPI оплачен ($19.44, авто-топап) → музыка/видео/3D разблокированы.

## ПРАВИЛА РАБОТЫ (для всех сессий — чтобы грызть нон-стоп без Damir)
1. НЕ останавливайся спрашивать. Безопасные дефолты. Решения по деньгам/процессору — пропусти
   (Damir отложил оплату), всё остальное решай сам.
2. Замок `.agent-locks/` перед правкой файла ВНЕ своей ланы / общего файла (chat.tsx, select.ts,
   server.py). BUSY → возьми другую задачу из своего блока.
3. После КАЖДОЙ правки: `npx tsc --noEmit` (фронт, exit 0) / `python3 -c "import ast;ast.parse(open('server.py').read())"` (бэк). Не двигайся с красным.
4. Каждую фичу ПРОВЕРЯЙ вживую (preview_eval/curl/скриншот), не на глаз. «Сделано» = доказано.
5. Heartbeat в STATUS-<id>.md раз в ~15 мин. Бинарный статус по своему чек-листу.
6. Дойдя до конца блока — возьми следующий приоритет из ОБЩЕГО беклога внизу (не простаивай).

## DEFINITION OF DONE (v1) — финиш, когда ВСЁ зелёное. Приоритет: 🅜 must · 🅝 nice · ⛔ отложено
Источник целей: PARITY-MATRIX.md (78 скринов Damir).
- [ ] 🅜 Чат: мульти-модель + роутинг + circuit-breaker + кросс-провайдер (✅ есть)
- [ ] 🅜 **Tools-меню** (ChatGPT): Web Search / File Search / Skills / Run Code / Artifacts — одной «+», тумблеры
- [ ] 🅜 **Режимы** (Grok): Fast / Auto / Expert / Heavy — чистый селектор (быстрая/авто/думающая/совет-мозг)
- [ ] 🅜 **Слайдер «память: последние N сообщений»** (RedPill) + компакт старого через /api/compact
- [ ] 🅜 **MCP-коннекторы маркетплейс** (LibreChat): фреймворк + 3-5 именованных коннекторов (вкл/выкл/фильтр)
- [ ] 🅜 **Auxiliary-модели на под-задачу** (Hermes): дешёвая модель на Vision/Web-extract/Compression/Title; дефолт «main». Экономия.
- [ ] 🅜 **Skills-библиотека по категориям + тумблеры** (Hermes) — апгрейд skill-builder в реестр
- [ ] 🅜 Память: на-чат правится (✅) + кросс-чат профиль (✅) + Memories-панель
- [ ] 🅜 Студия: img(✅)/video(✅)/cinema(✅) + **music** + **3D** + **P0-сброс ИСПРАВЛЕН**
- [ ] 🅜 БАГ-СВИП (со скринов): фон-протекание студии · студия-retry «не вышло» · оркестратор-таймаут · имена-моделей галлюцинация · подтвердить что фолбэк-баннер ушёл (circuit-breaker)
- [ ] 🅝 Темы/палитры (Hermes) · Voice STT мультипровайдер · Agent-галерея polish · Prompts library · Bookmarks
- [ ] 🅝 MUAPI playground на модель (Form/JSON/цена/Embed) + AI-tools сетка с ценами
- [ ] 🅜 Качество: только рабочие модели (✅), понятные ошибки (✅), мобайл-проход, smoke 0 fail
- [ ] ⛔ Storefront вход (auth.py готов) — можно, но не блокер; оплата ОТЛОЖЕНА

---

## РАСПРЕДЕЛЕНИЕ — 5 сессий, параллельно, нон-стоп

### S1 · CORE-A (kedr) — chat.tsx surface
1. 🔴 **P0: фикс сброса студии** — устойчивый state режима студии + stable key (студия не прыгает в «Картинки»). ВОЗЬМИ ЗАМОК на chat.tsx, не затри routing/circuit-breaker правки (logicalModelKey/modelCopies/healthV/fundedModels/providerFunded).
2. **Tools-меню как ChatGPT**: кнопка «+» → File Search (RAG-тумблер) / Web Search (супер-поиск) / Skills / Run Code / Artifacts. Все бэкенды ЕСТЬ — это surfacing в один аккуратный поповер.
3. **Режимы как Grok**: чистый селектор Fast/Auto/Expert/Heavy (маппинг: Fast=быстрая не-думающая, Auto=авто-роутер, Expert=думающая/deep, Heavy=совет/мозг-оркестратор).
4. **Слайдер «память чата: последние N сообщений»** (как RedPill, 307): кап истории в запросе, старое — компакт через /api/compact. Юзер двигает ползунок.
5. Баг-свип в своей зоне: фон-радуга протекает по UI студии (контраст/маска) · подтверди что «Основная модель недоступна» баннер больше НЕ на каждом сообщении (circuit-breaker уже в коде).
DoD: всё кликается, переключается, проверено в браузере, tsc=0.

### S2 · STUDIO (amur) — медиа (AIMLAPI funded!)
1. e2e-проверь AIMLAPI-видео через живой UI (MP4 + биллинг).
2. **Музыка** (AIMLAPI MiniMax ~$0.039/трек): пикер + генерация + charge_media. Вкладка «скоро»→рабочая.
3. **3D** (AIMLAPI): генерация + биллинг. Вкладка→рабочая.
4. Балансо-осознанный выбор медиа-модели + понятные ошибки. Taste-learning (опц.).
НЕ трогай chat.tsx (kedr). DoD: music/3D/video отдают реальный результат из UI, проверено.

### S3 · BACKEND (ural) — MCP-маркетплейс + движок
1. **MCP-коннекторы маркетплейс** (главная дыра vs LibreChat): фреймворк включения/выключения именованных коннекторов + 3-5 рабочих (публичные/крипто-дружелюбные: DeepWiki/Gitmcp/HuggingFace/поиск; OAuth-коннекторы типа Gmail/Stripe — каркас + «нужен аккаунт»). server.py /api/mcp расширить.
2. **Auxiliary-модели на под-задачу** (Hermes): дешёвая/быстрая модель на Vision, Web-extract, Compression (компакт истории), Title-gen, Skill-search — реестр с дефолтом «main», переопределяемо. Прокинуть в chat()/route_model/relay. Большая экономия токенов.
3. Медиа-эндпоинты (api_video/image/audio) роутить балансо-осознанно (как чат: пустой провайдер мимо).
4. Движок зелёный: circuit-breaker/роутер/балансо-кап стабильны, /api/init 200, AIMLAPI poll стабилен.
5. Баг: оркестратор-таймаут «долго не отвечает» — увеличь лимит/стриминг-прогресс; галлюцинация имён моделей в подсказках — чисти из живого каталога.
DoD: коннекторы вкл/выкл работают на 3-5 · auxiliary-модели применяются (curl-лог показывает дешёвую на под-задаче) · curl-проверено.

### S4 · CORE-B (routing/atlas-линия) — библиотеки + внешний вид
1. 🅜 **Skills-библиотека по категориям + тумблеры on/off + поиск** (как Hermes): апгрейд skill-builder в реестр (Юрист/Переводчик/Программист… уже есть как заготовки — собери в категоризированную панель с переключателями).
2. 🅝 **Темы/палитры** (Hermes: Dark/Light/System + Midnight/Cyberpunk/Slate, живой превью) · **Voice STT** (mic→текст, мультипровайдер).
3. 🅝 **Prompts library** + **Bookmarks** (закладки сообщений) + **Memories-панель** (вынести редактор памяти-на-чат).
4. 🅝 **Экран входа** (auth.py готов) — если успеешь; НЕ блокер, оплату НЕ делаем.
5. Кросс-сессионный баг-свип по реальным флоу (через замок, не в чужой активной зоне).
DoD: skills-реестр с тумблерами работает · темы переключаются · tsc=0.

### S5 · COORDINATOR — интеграция и финиш-гейт
1. Держи AGENTS-BOARD + замки чистыми, разруливай конфликты ланов.
2. Каждые ~30 мин: smoke по всем флоу (чат/студия/поиск/браузер/агенты/память/MCP) → STATUS.
3. Сверяй прогресс с Definition of Done выше; помечай зелёным только проверенное.
4. Когда все чекбоксы DoD зелёные → СТОП, отчёт Damir «v1 done».

---

## 🧠 SELF-KNOWLEDGE — Тайга ЗНАЕТ СЕБЯ изнутри, up-to-date (🅜 must, запрос Damir)
Цель: Тайга всегда в курсе своей архитектуры (режимы / команды / агенты / связки-relay / brain /
модели-роутинг / память / MCP / студия) и КАК создавать новое — чтобы помогать юзеру (и себе)
строить агентов/скиллы/связки/функции, как Claude оркеструет сессии. Самоописание АВТО-генерится
из кода → НЕ устаревает (не хардкод).
- **BACKEND (ural):** `self_manifest()` — интроспекция ЖИВЫХ реестров (command registry, modes,
  DEFAULTS-модели 5 ролей, agent/skill/relay/brain механизмы, MCP-коннекторы, память, студия-модальности)
  → компактный структурный манифест «что умею + КАК создать каждое». Регенерится на старте/
  catalog-refresh → всегда актуально. Инжект КРАТКО в system-промпт (или retrievable «self»-тул через RAG).
  На «что ты умеешь / как сделать агента для X / какая модель лучше для Y» — Тайга отвечает из
  РЕАЛЬНОГО текущего состояния, не выдумывает.
- **CORE (kedr/sosna):** «self-build» — на просьбу «создай функцию/агента/связку» Тайга скаффолдит
  её через agent-builder/skill-builder, который она знает. (#12 само-улучшение из GAP-анализа.)
DoD: спроси Тайгу «как создать агента-переводчика сленга» — отвечает точными шагами из своих реальных
возможностей (не выдуманными) и предлагает собрать.

## 🗣 НИШЕВОЕ ПОНИМАНИЕ — сленг/жаргон (🅝 nice, запрос Damir)
- Готовый скилл «Сленг» в skills-реестре (sosna): понимает/объясняет/переводит RU+СНГ сленг,
  интернет-сленг, мат, жаргон. Uncensored-модели это тянут — нужен явный скилл + инструкция в систему.
- Опц.: ниша-словарь как RAG-док (актуализируемый), чтоб ловить свежий сленг.

## 🔁 НАЙДЕНО ПРИ СВЕРКЕ ИСТОРИИ — ЗАБЫТОЕ (вписано 2026-06-12, сессии добирают)
Damir просил, я упустил. Раскидано по ланам:
- 🅜 **`/uncensor` команда** (BACKEND+CORE): функция-переписчик промпта → любая модель (даже
  цензурная) даёт uncensored-эквивалент для кода/инфо. Бэкенд: rewrite_uncensored(prompt). CORE: команда /uncensor.
- 🅜 **`/pipeline` — юзер собирает цепочки моделей** (CORE) — ГЛАВНАЯ фишка по Damir: визуальный
  конструктор модель→модель→модель (/new + /pipeline). sendPipeline в бэке ЕСТЬ — нужен юзер-UI билдер.
- 🅜 **Видео по референсу** (STUDIO): вход «референс-видео» → генерим в его СТИЛЕ + свой текст. Доп. к видео-студии.
- 🅝 **Тоггл-лимит трат на операцию** (CORE): «стоп после $X / экстра-макс» как в Claude Code — потолок расхода на дорогую операцию (поверх бюджета токенов).
- 🅝 **T9/опечатки + семантика** (BACKEND/skill): модель понимает кривые слова автокорректа + смысл-с-контекстом (что имелось в виду). Инструкция в систему + опц. нормализация. Шире, чем «сленг».
- 🅝 **MCP-коннектор при создании скилла/агента** (BACKEND+CORE): при сборке своего скилла прикрутить коннектор (ComfyUI и пр.); супер-фича native / всё с готовым MCP = коннектор.
- 🅜 **3 яруса Обычный / Студия / Ультра** (CORE — верх-навигация, Damir: «do all»): три чётких входа.
  • Обычный = чат/код + все тулзы + g-мозг + без цензуры под охраной. • Студия = картинки/видео/музыка/3D.
  • Ультра = премиум-супер-агент: топ-модель, генерит видео/картинки ВНУТРИ чата, полный no-refusal, супер-поиск+оркестратор по умолчанию. Реструктур верхнего таб-бара (Fast/Auto/Expert/Heavy остаётся осью УСИЛИЯ внутри яруса).
- 🅜 **БЕСПЛАТНЫЙ скилл-рендер тир — КАРТИНКИ + ВИДЕО кодом** (STUDIO+BACKEND): рендер БЕЗ платной модели.
  • КАРТИНКИ: диаграммы/постеры/инфографика/графики/SVG-арт/слайды/HTML→скриншот (canvas/p5/brandkit).
  • ВИДЕО: объяснялки/шортсы(TikTok/YT)/слайд-видео/дата-виз/анимации/туториалы — паттерн video_explainer:
    LLM пишет сцены-код → FFmpeg рендерит → MP4 БЕСПЛАТНО. ⚠️ ЛИЦЕНЗИИ: спина = **manim+MoviePy (MIT)**,
    НЕ Remotion (не бесплатен для команд 4+). Музыка = **ACE-Step 1.5 (Apache)**, НЕ MusicGen (CC-BY-NC=нельзя
    в платном!). Озвучка = **Kokoro-TTS (Apache)**, STT = faster-whisper (MIT). Картинки free = **FLUX.1 schnell (Apache)**, НЕ FLUX dev (non-commercial). Локальный движок = ComfyUI как СЕРВИС (GPL не линковать в код).
  Авто-роутер: «сделай картинку/видео» что по сути диаграмма/объяснялка/шортс → бесплатный код-рендер;
  фотореал/кинематограф/Suno-песня/AI-3D → платная модель (AIMLAPI). ОГРОМНАЯ экономия (объяснялки/шортсы = топ-спрос).
  Скиллы-кирпичи: remotion · manim-video · canvas-design · brandkit · algorithmic-art. Репо-паттерн: prajwal-y/video_explainer.

## 🗄 МАСШТАБ — flat-JSON → SQLite (BACKEND/ural, Damir: «do this»)
Плоские JSON не выдержат поток юзеров (гонки, нет транзакций — audit#1). Миграция КООРДИНИРОВАННАЯ
и ОСТОРОЖНАЯ (gap-анализ: «surgical вместо полной миграции — риск>польза»):
- SQLite (stdlib `sqlite3`) с WAL: таблицы users/chats/messages/balance/memory/gens — event-log стиль.
- АДАПТЕР: сохранить текущий API load_*/save_* поверх SQLite (не ломать вызовы) → миграция за фасадом.
- Транзакции на запись баланса/память (убрать lock-хак). Бэкап JSON перед миграцией. Тест: 50 параллельных → ровно сходится.
- Не блокер запуска (юзеров пока нет) — делать аккуратно, не ломая live.

## 🧩 УНИВЕРСАЛЬНЫЙ АДАПТЕР СКИЛЛОВ — вставил ссылку → Тайга ИНТЕРПРЕТИРУЕТ и ставит (🅜, Damir)
Не просто «скачать скилл», а: вставил ЛЮБОЙ github/skill-линк (Claude Code SKILL.md, GPT-скилл, ECC,
Cursor-rule, MCP-tool…) → **умный слой Тайги ЧИТАЕТ его, ПОНИМАЕТ намерение/тулзы/инструкции и
АДАПТИРУЕТ в НАТИВНЫЙ скилл Тайги** (маппит на наши модели/тулзы/команды). Работает даже если скилл
сделан под чужой движок — Тайга интерпретирует, а не копирует слепо (у неё все модели + свой слой).
- BACKEND (ural): `import_skill(url)` — фетч файла → LLM-конвертер «любой-формат → Taiga-SKILL» (
  frontmatter name/description + инструкция + нужные тулзы/коннекторы) → валидатор (skills-health стиль) → реестр.
- CORE-B (sosna): UI «вставь ссылку на скилл» в skills-библиотеке → превью адаптации → «поставить».
- Это «1000 скиллов из ссылок» Damir + harness-agnostic = сильный дифференциатор (ни у кого нет).
DoD: вставил ссылку на Claude-Code-скилл → Тайга показала адаптацию → поставила → скилл работает на наших моделях.

## 🧠 МОЗГ-ПАМЯТЬ: GBrain-концепт + TurboVec-сжатие (Damir-скрины)
- **GBrain** (Garry Tan, тот же автор gstack) = РОВНО наш план памяти: само-вяжущийся граф + слой
  синтеза (ответ с цитатами, не куча файлов) + ночное само-улучшение. Реализуем через Mem0+Letta+
  self-knowledge+sleep-time. СКВОЗНОЙ слой (все режимы). ПРЕМИУМ-версия (авто-ингест всего + ночной
  само-апгрейд) = **флагман ULTRA-яруса** (лицо премиума). BACKEND/ural + Ultra-UI.
- **TurboVec** (вектор-индекс, сжатие 16× 31GB→4GB, Rust+Python, MIT-проверить) = ИНФРА слоя
  эмбеддингов под памятью/RAG. Альтернатива/пара sqlite-vec когда вектора растут. 🅝 (оптимизация, не блокер).

## 🔌 ЮЗЕР САМ ПОДКЛЮЧАЕТ СЕРВИСЫ — BYOK + свои эндпоинты (🅜, запрос Damir)
«users can connect those things themselves» — сделать так, чтобы ЮЗЕР через UI подключал СВОЁ:
- **BYOK ключи** провайдеров (NanoGPT/Venice/Chutes/RedPill/OpenAI-совм.) — свой ключ = свой счёт/приватность (часть уже есть — довести до UI-формы «добавь свой ключ»).
- **Свои эндпоинты локальной генерации**: URL своего **ComfyUI** / своего TTS (Kokoro) / своего LLM-сервера →
  Тайга зовёт его эндпоинт. БОНУС ЛИЦЕНЗИИ: мы НЕ бандлим GPL (ComfyUI) — юзер крутит свой инстанс,
  GPL у него, не у нас. Ему — бесплатная генерация на своём GPU.
- **Свои MCP-коннекторы** (в маркетплейсе — юзер добавляет свой сервер по URL/команде).
- Проверка прав/изоляция: чужой ключ/эндпоинт — только в сессии юзера, не в общий пул (если не owner).
Ланы: CORE (UI «подключить сервис/ключ/эндпоинт» в настройках) + BACKEND (резолв своего эндпоинта/ключа в роутинге, BYOK-приоритет уже есть в resolve_key).
DoD: юзер из UI добавил свой ключ И свой ComfyUI-URL → генерит через них, проверено.

## 🚀 ЛИФТЫ ИЗ РЕП — ИНТЕГРИРОВАТЬ (🅜, Damir: «lets them use them»). Детали: LIFT-MAP-2.md / LIFT-MAP-3.md
Сессии: после текущего блока ВКЛЮЧИТЬ свои лифты (реально интегрировать, не отложить):
- **ural (BACKEND):** Mem0 (долгосрочная память, дроп-ин) · Letta-паттерн (само-правка памяти) ·
  sqlite-vec+sqlite-utils (миграция БД) · LLM-Guard-сайдкар (анти-инъекция, цензур-сканеры OFF) ·
  big-AGI Beam-движок (веер→слияние) · free-claude-code conversion+stream-recovery+heuristic-tools (роутинг) · RAG-Anything мультимодал.
- **kedr (CORE):** assistant-ui примитивы (generative-UI/tool-аппрув) · Beam-UI (веер моделей в одном виде).
- **amur (STUDIO):** ComfyUI-как-сервис · FLUX-schnell · ACE-Step (музыка) · Kokoro+faster-whisper (голос) · manim/MoviePy (free-видео).
- **sosna (CORE-B):** AnythingLLM workspace-изоляция · Voice-стек (Kokoro/whisper) · ECC SKILL.md-формат + claude-skills marketplace.json.
- BTCPayServer — когда расцензуришь оплату (отложено).
Каждый лифт: проверь ЛИЦЕНЗИЮ (MIT/Apache=код OK; GPL=сервис; ловушки в LIFT-MAP-3), сохрани нотис, проверь вживую.

## ОБЩИЙ БЕКЛОГ (бери, если свой блок закончил раньше)
- Мобайл-проход всех экранов (студия/чат/пикер)
- File Search как отдельный режим (RAG поверх загруженных доков)
- Onboarding/пустые состояния (первый запуск)
- Bookmarks/Prompts экспорт-импорт
- Озвучка через Venice (2-й TTS-провайдер)

## ОТЛОЖЕНО (Damir) — НЕ делать сейчас (юзеров нет, мы далеко от запуска)
- Реальная оплата (процессор+чекаут) — после теста
- **Гард-дипфейк** (блок face-swap на реальных людей) — позже, не зацикливаемся на безопасности пока
- Биллинг-модалка мульти-рейл · usage-аналитика · «что нового»-фид
- Соц-публикация (YT/TikTok/IG) · async-coding-PR · background-tasks панель · scheduled-routines UI
- OS-автоматизация скиллы (macos-use и пр. — мы веб-апп) · 3-тира реструктур
