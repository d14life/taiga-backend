# START-HERE.md — точка входа для чистой сессии пересборки Тайги

Привет. Ты — свежая Claude Code сессия, которой поручена **чистая пересборка Тайги: чистый код + фичи, которые реально работают как надо**.
Весь контекст уже собран тремя аудитами и лежит в файлах рядом. Тебе НЕ нужен ничей прошлый диалог.

> ⚠️ ОБЛАСТЬ РАБОТЫ (Damir, важно): СЕЙЧАС НЕ вывод в продакшен. **ОТЛОЖЕНО (не трогай пока):**
> — Мина #1 RU-платёж и весь биллинг-процессинг;
> — Фаза D «масштаб на 10k» (ASGI-сервер, Postgres/pgvector, объектное хранилище, бэкапы).
> **ФОКУС СЕЙЧАС:** Фаза B (strangler — чистая структура) + Фаза C-качество (сделать фичи настоящими/корректными,
> убрать баги, быстрые победы). Мина #0 (безопасность) — дешёвая гигиена, сделай, но НЕ блокер (локально безопасно).

## 1. ПРОЧИТАЙ ЭТИ ФАЙЛЫ (в этом порядке) — это вся правда о системе
0. **`docs/MANIFEST.md` — ГЛАВНАЯ точка входа (новее всего): законы, память-MCP, источники фич
   (docs/design/FEATURE-LEDGER.md = 139 фич), 2 дизайна, поток сборки, оценка кода. Читай ПЕРВЫМ.**
1. `CLAUDE.md` — правила, стек, рабочий цикл, какие скиллы под что, анти-RCE.
2. `REBUILD-BRIEF.md` — философия (strangler-fig, НЕ rewrite) + целевая архитектура + 14-шаговый порядок выноса + DoD.
3. `ARCHITECTURE-MAP.md` — карта текущего кода: точки входа подсистем, ТОП-10 структурных косяков, флаги безопасности.
4. `docs/design/FEATURE-LEDGER.md` — ГЛАВНЫЙ реестр (139 сверенных фич) + `FEATURE-INVENTORY.md` (~184 буллета). Чек-лист «не потерять ничего».
5. `GAP-AUDIT.md` — «код vs как должно быть»: скоркарта A–F, разрывы, фиксы по каждой фиче.
6. `PHASE-A-C-HARDENING.md` — приоритезированный бэклог закалки (2 мины → быстрые победы → критичное → …).
7. `taiga-web/AGENTS.md` — «это не тот Next, что ты знаешь».

**НЕ перезапускай большие аудиты — они уже сделаны и зашиты в файлы выше.** Максимум — дёшево перепроверь 2 мины grep-ом.

## 2. ПУТЬ ДО PRODUCTION-GRADE (5 фаз)
```
ФАЗА A [СРОЧНО]   → 2 мины: #0 подделываемый owner (RCE) + #1 RU-платёж. См. PHASE-A-C-HARDENING.md.
ФАЗА B [структура] → 14-шаговый strangler-вынос модулей. См. REBUILD-BRIEF.md.
ФАЗА C [закалка]   → 32-пунктовый бэклог GAP (безопасность+корректность+AI-фичи-по-настоящему). См. PHASE-A-C-HARDENING.md.
ФАЗА D [масштаб]   → ASGI-сервер + Postgres+pgvector + объектное хранилище + бэкапы + per-user шифрование.
ФАЗА E [процесс]   → PR-ревью (CodeRabbit) + мониторинг + нагрузочное.
```

## 3. С ЧЕГО НАЧАТЬ (порядок действий — пере-скоуп: НЕ продакшен сейчас)
1. **ФАЗА B strangler шаг 1** (удалить мёртвый код) → дальше по 14 шагам REBUILD-BRIEF. ЭТО ГЛАВНОЕ — чистая структура.
2. **Быстрые победы** из PHASE-A-C-HARDENING (часы каждая) — улучшают продукт сразу.
3. **ФАЗА C-качество** — сделать AI-фичи НАСТОЯЩИМИ (фьюжн с консенсусом, реальная верификация агента, реранк RAG,
   FTS-индекс памяти) + убрать баги. Это «фичи работают как надо».
4. **Мина #0 (безопасность, `resolve_caller`)** — дешёвая гигиена, сделай когда удобно. НЕ блокер (локально безопасно).
5. **ОТЛОЖЕНО — не трогай пока:** Мина #1 платёж/биллинг · вся Фаза D (ASGI/Postgres/хранилище/бэкапы/масштаб 10k).

## 4. КАК РАБОТАТЬ КАЖДЫЙ ПУНКТ
- Прочитай реальный код (точные строки в ARCHITECTURE-MAP / GAP-AUDIT).
- Перед нетривиальным — `superpowers:brainstorming`. Перед фиксом бага — `superpowers:systematic-debugging`.
- Сделай маленький шаг. **Поведение не меняй во время выноса** (FEATURE-INVENTORY = чек-лист).
- **ПРОВЕРЬ** (`superpowers:verification-before-completion` — доказательства, не слова): tsc=0 / ast.parse / /app=200 / смоук.
- **КРИТИЧНЫЕ правки (мины, платёж, авторизация) — адверсариально проверь ВЕЕРОМ ревью-агентов** (`superpowers:dispatching-parallel-agents` или `code-review`): 2-3 независимых рецензента ПЫТАЮТСЯ опровергнуть фикс ДО коммита. Это твой «совет рецензентов».
- ТОЛЬКО зелёное → коммит (сначала `taiga-web`, потом корень) → push. После `server.py`: `launchctl kickstart -k gui/$(id -u)/com.taiga.backend`.
- Отметь [x] в соответствующем файле-чек-листе. Доложи Damir бинарно.

## 5. У ТЕБЯ ЕСТЬ ВСЁ НУЖНОЕ
- **Скиллы и агенты** — глобальны для этой установки Claude Code, у тебя есть ВСЕ: дизайн (`refero-design`/`impeccable`/`frontend-design`), `superpowers:*` (brainstorming/systematic-debugging/TDD/verification/dispatching-parallel-agents), `code-review`, `security-review`, `supabase`. Какой под что — в CLAUDE.md.
- **Параллельные агенты** — для независимых выносов (разные новые файлы) строй параллельно, интеграцию в общие файлы делай ОДНИМ проходом по очереди.
- MCP-серверы с логином (refero) могут попросить переподключить — это норм.

## 6. КОГДА ОСТАНАВЛИВАТЬСЯ И СПРАШИВАТЬ DAMIR
Работай автономно, доклад бинарно после каждого пункта. СТОП и спроси только на: выборе платёжного процессора (#1),
любом решении, меняющем ПОВЕДЕНИЕ/фичу, или развилке, которую сам не решишь. Иначе веди работу до конца.

## СТАРТ
Прочитай файлы из п.1. Затем начни с **Фазы B, шаг 1 (удалить мёртвый код)** — чистая структура прежде всего.
Платёж и масштаб на 10k — ОТЛОЖЕНЫ (сейчас не продакшен). Поехали.
