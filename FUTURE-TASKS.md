
## Damir-requests (записано в марафон-сессии)
- DESIGN MODE: скопировать/адаптировать «design mode» из github.com/nexu-io/open-design.
  КОГДА: ПОСЛЕ того как студия + основной чат доведены. ВАЖНО: сперва проверить ЛИЦЕНЗИЮ репо —
  не копировать verbatim если не permissive (MIT/Apache). Если permissive → адаптировать с атрибуцией;
  иначе → построить свою версию по мотивам (не копия).
- FILES-дерево: хранить файлы ТАМ, где крутится терминал (E2B-песочница, 10ГБ/песочница).
  → Files-вкладка листает ФС песочницы (ls), для владельца — локальный shell. Нужна per-chat сессия песочницы.
- A: терминал ↔ ИИ live (поток tool_result → терминал) — нужен tool-event stream в панель.
- C: морф (чат сам → агент) — большой финал.

## ОБЩАЯ ПЕСОЧНИЦА (Damir, ключевая идея — объединяет Files + A + терминалы)
ОДНА персистентная E2B-сессия НА ЧАТ, общая для ИИ и юзера:
- ИИ run_code И терминал юзера бьют в ОДНУ песочницу → общая ФС, общее состояние.
- Файлы юзера + файлы ИИ в одной песочнице → оба видят и используют ("user files on sandbox so AI can use, user can use too").
- 2 терминала одновременно (ИИ + юзер) в одной сессии = co-coding.
- Память пишется туда же (доступна и ИИ, и юзеру).
Что нужно: lifecycle персистентной E2B-сессии per chat (создать/держать/убить по таймауту 1ч),
маппинг chat_id → sandbox_id, run_code/terminal/files/A — все через эту сессию. Реальный арх-билд.
