# Overnight build queue — Design + Agent → «as cool as open-design»

Rule each wave: take the TOP unchecked item → build with parallel agents (each agent on DISTINCT new/isolated files; I own chat.tsx integration) → integrate → VERIFY (frontend `npx tsc --noEmit`=0, `python3 -c "import ast;ast.parse(open('server.py').read())"`, `curl -o/dev/null -w %{http_code}` /app =200, smoke any new endpoint) → ONLY IF ALL GREEN: commit (frontend then backend pointer) + push → check the item → append a binary line to STATUS-claude-overnight.md. Never commit broken code. When all checked → STOP "design+agent done".

Repo: /Users/damir12/Downloads/claude-sessions/2026-06-10/mostik-ai ; frontend = taiga-web ; design aesthetic = match design-system-studio.tsx / design-canvas-workspace.tsx ; open-design source = /Users/damir12/Downloads/claude-sessions/2026-06-14/harness-analysis/open-design/apps/web/src/components. License: open-design Apache-2.0 + paperclip/ralph MIT (reimplement concepts, our own code; agentic-os CC-BY-NC = ideas only, never copy files).

- [x] **1. Inline unfenced-HTML → live preview.** In markdown.tsx, when an assistant message contains a full `<!doctype html>`/`<html>` doc with NO code fences, route it through the existing `extractArtifact` + `<WebPreview>` inline card (today only fenced ```html triggers it). So every generated site shows as a live preview, never raw HTML text. New helper file if needed; minimal markdown.tsx edit.
- [x] **2. Design templates gallery.** New `design-templates.tsx`: a grid of ~8 quick-start design briefs (landing, dashboard, pricing, auth, blog, portfolio, app-shell, email) each with a thumbnail + one-tap → opens DesignCanvas with that brief. Mirror open-design's HomeTemplatesReveal concept. Wire into Design «Создать» as a strip above the brief box.
- [x] **3. Agent runs sidebar (paperclip concept).** New `agent-runs-panel.tsx`: a unified list of agent runs (from automations + tasks + loops localStorage), status badge, pause/resume/cancel where applicable, last-run time, output peek. Add as Агент → «Прогоны» sub-tab. MIT-safe, our code.
- [x] **4. Ralph monitor (verify→retry→decompose).** New `agent-ralph-monitor.tsx`: visualize a loop run as iterations with per-step verify/retry/decompose state + iteration counter + session notes. Wire into the loop-studio run view or as a panel. MIT-safe concept, our code.
- [x] **5. Backend automation scheduler.** Make Автоматизации actually fire: a lightweight per-user schedule store + a background thread in server.py that wakes each minute, runs due routines via our agent path, records a run. Honest, real cron — replaces the "once background scheduling is on" note. Backend-heavy; gate safely, owner-first.
- [x] **6. Slides-in-canvas.** Real slide-deck preview inside DesignCanvas (kind:"slides") — generate an HTML reveal-style deck and show it live with prev/next, instead of pptx-to-chat. New `slides-canvas.tsx` + extend DesignCanvas kind union.

Done items get checked + a STATUS line. Stop when all 6 checked.
