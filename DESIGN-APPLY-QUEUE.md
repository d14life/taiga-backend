# DESIGN-APPLY-QUEUE — дизайн-правки Damir к применению на public/shell.html

Источник: `DESIGN-REQUESTS-RECOVERED.md` (полные цитаты) + новые по ходу. Применять, НЕ драться за shell.html с активными агентами (иконки/флот). Каждая правка — с пруфом-скрином. Последнее слово Damir = выше по приоритету. Дизайн макета — эталон, не ломать раскладку.

## Очередь
1. [🔁 ПЕРЕДЕЛАТЬ — ФИНАЛ Damir 2026-06-17] **Иконки → icons8 «Liquid Glass COLOR» (ЦВЕТНОЙ пакет `liquid-glass-color`, 2678 икон).** Damir: серый/no-color + Apple-перекрас «look worse» → ВЕРНУТЬ цветной Liquid Glass Color пак, передёрнуть ВСЕ иконки заново из него. **СУПЕРСЕДИТ: no-color glass И Apple-мультицвет-перекрас.** Полное покрытие (все ~78 ключей + app/feature-иконки). Тянуть `platform=liquid-glass-color` через icons8-MCP; вернуть цвет тайлам `.iost` (убрать монохром-CSS, что ставил no-color агент). ⚠️ квота icons8: ~78 уже потрачено в этом месяце — передёргивание ещё ~78 может упереться в лимит → если так, тяну приоритетные (столы+ключевые) и докладываю.
   **ОБВОДКА — УБРАТЬ (Damir передумал 2026-06-17):** тёмная обводка/контур (drop-shadow «дефиниция», добавлена в `feb176a`) НЕ нравится → СНЯТЬ это CSS-правило. Оставить НАТУРАЛЬНЫЙ liquid-glass-color вид иконок как в дизайне. Бейджи (тоже `feb176a`) — ОСТАВИТЬ. Сделать, как только responsive-агент Фазы-2 освободит `shell.html`.
2. [⬅ NEXT] **Размер шрифта и иконок — 3 ступени, как на iPhone.** В Настройках: сегмент «Шрифт» (Маленький/Средний/Крупный) + сегмент «Иконки» (Маленький/Средний/Крупный), iOS-стиль segmented. Масштабирует текст и иконки по ВСЕМУ UI; persist per-user (localStorage + userconfig, грузить на старте). Реализация: `data-fontscale`/`data-iconscale` на `:root` + scale-CSS (через CSS-переменные/`zoom`, НЕ `transform` — чтоб не плыла раскладка). Не ломать вёрстку макета.
3. **Вордмарк-hero → янтарный градиент #FFB088→#FF9E64**, плавная волна слева-направо, мягкие края — НЕ радуга, НЕ мигание целиком (сейчас в макете радужный — поправить).
4. **Лого на КАЖДУЮ модель и компанию** (пикер моделей + дропдаун); иконки везде, без пустых мест, без emoji.
5. **Liquid-glass сильнее** на кнопках/пилюлях/композере.
6. **ДОК (Damir: «не забудь docker»)** — в макете реализован ПОЛНОСТЬЮ (`#dock`: закреплённые апп-иконки, разделитель, открытые окна, iOS-jiggle+удаление, drag-чтобы-закрепить, hot-zone), НО по умолчанию ПУСТОЙ и СПРЯТАН (уезжает за экран, `.show` нужен триггер) → выглядит «удалённым». ВОССТАНОВИТЬ: док ВИДИМ по умолчанию, наполнен дефолт-иконками, **центрирован вокруг чата** (не перекрывает композер), drag-добавить-фичу из библиотеки, jiggle+long-press удаление, возврат в библиотеку. Делает `desktop.js` (флот) — ПРОВЕРИТЬ, что реально показывается и работает.
7. **Ресайз** (хроническая боль Damir): 8 хэндлов на ВСЕХ окнах вкл. композер/окно-ответа/центр-модалки; кнопка-замок + перекладка зон.
8. **iOS-флюид**: spring-drag окон, серая подсветка drop-зоны, drag-drop файлов в чат/Студию/Терминал.
9. **Шрифты/фоны**: 3 шрифта по режимам (код = VS-Code-вид); 5 живых фонов (Aurora+Starfield хвалил), фон генерится через Студию; hero по имени/памяти.

10. **АУДИТ «дизайн не лучший — что запатчено/убрано» (Damir 2026-06-17):** текущий живой `shell.html` — НЕ финальный-лучший вид; в ходе сборки часть макета запатчена/не активна (док — пример: есть, но спрятан/пуст). ЗАДАЧА: пройти живой апп против ПОЛНОГО макета-эталона и восстановить ВСЁ неактивное/урезанное — док (видимость+наполнение), macOS-окна (drag/8-resize/светофор/тайлинг зелёной), flyout-панель рельса, jiggle, hot-zone, и пр. Живой апп обязан показывать ПОЛНЫЙ macOS-дизайн, а не урезанный. Скрин-сверка по каждому элементу.

11. **Приложение «Заметки» (как Apple Notes — Damir показал скрин, 2026-06-17)** — окно-апп в macOS-оконной системе (светофор + тулбар), с функционалом Apple Notes: слева список заметок (группы «Сегодня»/папки, заголовок + время + превью), справа редактор с панелью (**Aa-форматирование · чек-лист · таблица · скрепка-вложение · шеринг**), поиск, кнопка «новая заметка». Бэк ЧАСТИЧНО есть (per-user notes store: `load_notes`/`tool_save_note`/`_db notes`, возвращается в init) — но это инструмент для ИИ, нет user-CRUD. ДОБАВИТЬ: эндпоинт `/api/notes` (list/save/delete поверх существующего стора) + построить окно-апп; открывается из дока/рельса. Это фундамент «Тайга = macOS-подобная ОС с апп-окнами» — дальше можно добрать ещё утилит-аппов в том же духе.

12. **Figma macOS/iOS дизайн-ассеты (Damir, 2026-06-17) — РЕФЕРЕНСЫ + иконки:**
    • **iOS App Icons** `5XflMZcyjqt1KA3dEKpZbf` — 231 цветных апп-иконки → апп/док-тайлы + лого моделей/компаний. **ПРАВИЛО: где Figma-иконки нет → дефолт icons8 «Liquid Glass» (no-color).** Доступ: достаётся только Cover → нужен «Get a copy» в аккаунт Damir + новая ссылка. Трейдмарки чужих брендов — осторожно (лого AI-провайдеров ок).
    • **MacOS Frame Template** `xIBwJnWK7Lhmytd9biTziB` — рамка окна: топ-бар + светофор (Close/Min/Expand 24px), light+dark. Референс window-chrome. ✅ доступен.
    • **macOS Monterey** `E1brGUN7CKD4hYU9ShaIBu` + **Big Sur** `QIkU45bgbCDXl6CzV0Zopc` + **Ventura** `rShBJ9RHzF2gsck3nD6plN` UI Kits — полные macOS-компоненты (окна/кнопки/меню/сайдбары/иконки) для аутентичного вида. ВИЗУАЛЬНЫЙ референс (скрин фреймов → матчить в CSS), + можно тянуть готовые clean-иконки из них. Community → дубликат при нужде.
    • **СТИЛЬ ИКОНОК — ВЫБРАНО Damir: «Apple-мультицвет глифы».** Каждую glass-иконку залить ЧИСТЫМ Apple/macOS-цветом по смыслу (Чат-синий #0A84FF / Студия-розовый #FF375F / Дизайн-фиолет #BF5AF2 / Агент-индиго #5E5CE6 / Код-графит #8E8E93 / Память-оранж #FF9F0A / Ultra-янтарь #FF9E64 …), СОХРАНИВ glass-глубину (цветные полупрозрачные градиенты). Правится в `public/icons/glass/glass-icons.js` (+ цвет-карта `GLASS_TINT`). Тайлы `.iost/.dapp` — если останутся монохромными, нужен мелкий CSS-tweak в shell.html (следом).

13. **Апп «Файлы» (Finder-стиль, Damir: «please»):** окно-менеджер над готовым `userfiles`-стором (просмотр/переименование/удаление загруженных файлов), в пару к «Заметкам». Бэк `/api/userfiles` есть → построить окно-апп, открывается из дока.

14. **USE Figma mockup-иконки (Damir, ЭМФАТИЧНО «use them!!!», 2026-06-17):**
    • **Иконку Настроек → iOS-шестерёнки** (скрин Damir: macOS/iOS Settings gears). Заменить в рейле/доке/настройках. Источник: iOS-кит или icons8 iOS-settings (точная шестерёнка).
    • **Notification-бейджи (красный круг + число)** на иконках, что несут счётчики/уведомления (как «2» на Настройках); + **АВТО-бейдж, когда async-задача ГОТОВА** (картинка/видео/агент-ран done) — macOS/iOS-стиль. Логика: badge-API на dock/rail-иконку + хук на done-события (studio/agent SSE → badge).
    • **Реально тянуть иконки из Figma-китов** где доступно: iOS 16 UI Kit `VoU4457TTkgsjBBhRUigjn`, iOS 18 App Icons `0JR5IwOIkV8rCRZe54Amss` (+ iOS App Icons `5XflMZcyjqt1KA3dEKpZbf`) через `download_assets`; где только Cover — дубликат Damir. ⚠️ Figma-MCP сейчас в ЛИМИТЕ Starter-плана — выгрузка заблокирована до сброса/апгрейда.
    • **Спец/апп-иконки (Damir: «take note, camera and other from mockup, ~100 icons»):** note=Заметки · camera · settings=шестерёнки · files · + app-тайлы дока. ПЛАН: тянуть из icons8 `liquid-glass-color` СЕЙЧАС (тот же цветной стиль, без Figma-лимита) → ТОЧНЫЕ mockup-иконки из iOS-китов подставить, когда Figma-лимит сбросится / апгрейд. Фолдится в icon-finishing волну (после redo+polish).

## ➡️ ФАЗА 2: ФИНИШ ВЕБ-АППА (Damir 2026-06-17: «after this finished the web app»)
Когда иконки + дизайн доведены — довести `shell.html` до НАСТОЯЩЕГО продакшн веб-аппа:
1. Смонтировать `shell.html` как ОСНОВНОЙ маршрут — стать реальным `/app` (лицо продукта, не демо `/shell.html`).
2. Responsive: веб + MacBook + телефон (на <768px окна → sheets, док/рельс адаптируются).
3. Добить partial из coverage-флота: кнопка «Сгенерировать макет» (Дизайн) → /api/chat · отрисовка картинок в галерею · voice платн. озвучки · ввод в терминал-окно · поиск чатов (UI).
4. ENDLESS-цикл: юзер-кейсы (Playwright) → скрин каждого шага → анализ против эталона → fix → по кругу, пока не perfect и ГОТОВО к реальному юзер-тесту → `BUGS-FOUND.md` → волны фиксов.
5. Прод-готовность: security-остатки + перф + деплой.

Полные детали и дословные цитаты: `DESIGN-REQUESTS-RECOVERED.md`.
