# STATUS — kedr (CORE lane)

Старт 06:30. Лана CORE (фронт/чат/состояние). server.py — лана BACKEND (atlas), не трогаю без замка.

## Бэклог CORE — бинарно (done/not-done)

### ✅ Сделано (этой и прошлыми сессиями, проверено)
- [x] 3 режима дают разный вывод (composeSystem + MODE_SYS)
- [x] Связка→«Улучшить» + отдельный «Мозг» чип (взаимоисключающие)
- [x] «Решает сам» — decision.ts + decision-card.tsx + темперамент в настройках (риск-гейт инструментов)
- [x] Дерево сообщений — msg-tree.ts + ‹2/3› переключатель, правка/форк/regenerate → ветки, персист
- [x] Память: recall.ts (локальный смысловой поиск) + rag.ts (серверный RAG-гибрид) + тумблер в настройках
- [x] Кросс-чат профиль (profile.ts) + мастер-промпт
- [x] Лестница прав (план/авто/полный) + Shift-Tab
- [x] Поиск по чатам (sidebar)
- [x] Студия не слетает при ре-рендере (keep-alive + memo) — баг закрыт
- [x] In-chat метеринг картинок (server.py, до раздела лан)

### 🔴 Не сделано — бэкенд ГОТОВ (atlas), нужен только UI-провод
- [x] **Супер-поиск UI** ✅ ГОТОВО — прокси /api/supersearch + lib/supersearch.ts + components/sources-panel.tsx +
      команда /search + рендер SourcesPanel под ответом + поле sources в ChatMessage/msg-tree.
      Бэкенд: 5 движков, 13 источников (curl-проверено). UI: панель «Источники · N» с кликабельными
      нумерованными источниками + инлайн [1][2][3] в ответе (браузер-проверено, скрин). tsc 0.
- [x] **Браузер-в-чате панель** ✅ ГОТОВО — прокси /api/browser + lib/browser.ts + components/browser-panel.tsx +
      команда /browse. Панель: адрес-бар, ЖИВОЙ снимок Playwright, клик по снимку→координаты на бэк,
      кликабельные ссылки→переход, прокрутка/назад/текст, закрытие→освобождает сессию.
      Браузер-проверено: открыл example.com (снимок 1280×800), клик «Learn more»→перешёл на iana.org. tsc 0.
- [x] **Оркестратор-таймлайн** ✅ ГОТОВО — прокси /api/orchestrate + lib/orchestrator.ts + components/orchestrator-panel.tsx +
      команда /orchestrate (имя «agents» занято фоновыми-агентами «скоро» → переименовал). Панель: поле задачи →
      ПЛАН (декомпозиция мозгом) → АГЕНТЫ параллельно (Исследователь/Критик, модель, статус, результат) → ФИНАЛЬНЫЙ СИНТЕЗ.
      Браузер-проверено: «2 плюса солнечных панелей» → план из 2 + 2 агента (gemini-2.5-flash) + markdown-синтез (скрин). tsc 0.
- [x] **₽ везде + низкий баланс красным** ✅ ГОТОВО — прокси /api/init + lib/init.ts (тянет курс→money.ts setRate +
      кошельки провайдеров) + sidebar: баланс в ₽ (живой курс 72.29, $ вторично) + ряд провайдер-кошельков
      (Venice/NanoGPT/Chutes/Redpill) с low-флагом красным (только владельцу).
      Браузер-проверено: «Баланс 372 ₽ ($5.14)», NanoGPT 49,2 ₽ КРАСНЫЙ (низкий $0.68), остальные зелёные. tsc 0.
- [x] Пресеты (модель+промпт) — ✅ см. журнал ~09:30
- [x] Папки чатов — ✅ см. журнал ~10:10
- [ ] Темы (ночь/день/слейт) — не делал (низкий приоритет «за это платят»; чат уже тёмный)

## Журнал (новые внизу)
- 06:30 — клеймнул CORE (id kedr), записался на доску. Прочитал focus/lift-map. Пишу план.
- ~07:00 — ✅ СУПЕР-ПОИСК UI готов и проверен (прокси+lib+SourcesPanel+/search). Бэкенд 5 движков/13 источников.
- ~07:25 — ✅ БРАУЗЕР-В-ЧАТЕ (co-browse) готов и проверен (прокси+lib+BrowserPanel+/browse). example.com→iana.org клик работает.
- ~07:55 — ✅ ОРКЕСТРАТОР-ТАЙМЛАЙН готов и проверен (ural стабилизировал /api/orchestrate). План→агенты→синтез, скрин.
- ~08:20 — ✅ ₽ ВЕЗДЕ + НИЗКИЙ БАЛАНС КРАСНЫМ готов и проверен (прокси /api/init + lib/init + sidebar). NanoGPT красный, скрин.
- ~08:40 — ✅ CO-BROWSE → РАЗБОР СТРАНИЦЫ ИИ (глубина): кнопка «ИИ» в браузер-панели шлёт текст страницы в чат как
  контекст-файл → ИИ разбирает (Comet/Operator-ценность). Проверено: example.com → ИИ верно описал «домен для документации».
- ~09:00 — ✅ ДИСКАВЕРАБИЛИТИ: добавил Супер-поиск (тумблер) / Браузер / Оркестратор в «+»-меню (ChatGPT-стиль).
  Теперь 4 дифференциатора находятся без знания команд. Проверено: «+» → Браузер открыл панель.
- ~09:30 — ✅ ПРЕСЕТЫ (модель+промпт+режим+флаги одним кликом): lib/presets.ts (4 встроенных: Юрист/Кодер/Копирайтер/Аналитик)
  + components/presets-menu.tsx + чип «пресеты» в баре. Применить→настраивает режим+персону+модель+deep/relay/brain.
  «Сохранить текущее как…» создаёт свой пресет. Проверено: применил «Юрист РФ» (персона+стиль-чип), сохранил свой пресет. tsc 0.
- ~10:10 — ✅ ПАПКИ ЧАТОВ: lib/folders.ts + sidebar (folderId в мете, сворачиваемые секции, инлайн-выбор папки без
  всплывашек чтобы не обрезалось, «новая папка», удаление→чаты в «без папки», осиротевшие folderId→ungrouped).
  Проверено: создал «Проекты», переместил 2 чата (персист после перезагрузки), удалил папку→чаты вернулись. tsc 0.
- ~10:40 — ✅ MCP /connect: прокси /api/mcp (GET+POST) + lib/mcp.ts + components/mcp-panel.tsx + команда /connect
  (бэкенд add/remove/refresh уже был, owner-gated). Панель: форма подключения (имя+URL) + список серверов со
  статусом/инструментами + отключение. Проверено: панель показала РЕАЛЬНЫЙ сервер «refero» (зелёный) с его тулзами. tsc 0.
  (имя «connect» было занято soon-плейсхолдером — сделал его реальным, дубль убрал.)
- ~11:05 — ✅ КЛИКАБЕЛЬНЫЕ ИНЛАЙН-ЦИТАТЫ [1] (полировка супер-поиска до Perplexity): markdown.tsx (якоря #…-src-N →
  плавный скролл+подсветка, не новая вкладка) + lib/supersearch citeify() (превращает [N]→ссылку на источник) +
  рендер цитат в ответе с источниками. Проверено: [1][2][3] — кликабельные циан-чипы, клик→скролл+вспышка источника (скрин).
- ИТОГО зашито+проверено (10): супер-поиск+КЛИКАБЕЛЬНЫЕ ЦИТАТЫ · co-browse(+разбор страницы) · оркестратор · ₽/низкий-баланс · «+»-меню · пресеты · папки · MCP /connect.
- ОЧЕРЕДЬ ПУСТА. Весь CORE-бэклог из focus-листа закрыт. Дальше — по запросу Дамира или новые задачи с доски.

- ~11:25 — ✅ CO-BROWSE КУКИ: кнопка-ключ в браузер-панели → textarea для cookies.txt/JSON → браузить под
  СВОИМ логином (бэкенд parse_cookies принимает cookies.txt/JSON/list). Закрывает последний пункт CORE-списка
  координатора (браузер-панель «+поле cookies»). Проверено: ключ→textarea появляется. tsc 0.
  → ВЕСЬ CORE-список координатора (decision→дерево→₽→RAG→супер-поиск→браузер→оркестратор) ЗАКРЫТ ПОЛНОСТЬЮ.

- ~11:40 — ✅ HALF-WIRE FIX: команда `/branch` была «скоро»-заглушкой → сделал реальной (форк диалога через
  существующий forkFrom). Проверено: /branch → «Ветка создана», новый чат с копией сообщений, оригинал цел. tsc 0.

- ~12:00 — ✅ QUALITY/HARDEN ПРОХОД:
  • супер-поиск НЕ гоняется на тривиальных репликах («ок») — экономим 40-сек поиск + деньги (добавил !isTrivial гард).
  • БАГ: панели (браузер/оркестратор/MCP) были всегда-смонтированы → оркестратор с `started.current` не перезапускался
    на ВТОРОЙ задаче (показывал старый результат). Починил: панели монтируются на открытии (свежее состояние). Проверено: /orchestrate авто-стартует. tsc 0.
  • memMode (локальная/серверная память) — проверил: РЕАЛЬНО переключает recall (server→ragQuery с фолбэком на local), не half-wire.

- ~12:30 — ✅ HARDEN-2 (по итогам ЖИВОЙ проверки супер-поиска):
  • ЖИВОЙ /search проверен end-to-end: реальный grounded-ответ «OpenAI основана 11 декабря 2015» + 11 живых источников.
  • БАГ-РОБАСТНОСТЬ: фетчи superSearch/orchestrate/browser БЕЗ клиентского таймаута → при зависшем бэкенде (нагрузка
    3 сессий) фронт висел ВЕЧНО без фидбэка. Починил: AbortController-таймауты (поиск 75с / оркестратор 120с / браузер 45с)
    + мягкий фолбэк (поиск завис → отвечаю без веба). Это реально воспроизвелось при ре-ране — теперь не виснет.
  • Усилил промпт цитат (пример + «обязательно [N]») — модель не всегда ставила инлайн-[N]; citeify+рендер цитат уже проверены на сид-тесте.
- tsc=0 после каждого. Файлы Studio/ural не трогал.

- ~12:55 — ✅ HARDEN-3 (закрыл последние fetch-без-таймаута):
  • Новый общий хелпер lib/fetch-timeout.ts (fetchT: AbortController + жёсткий таймаут).
  • Обернул ОСТАВШИЕСЯ клиенты: rag.ts (ragQuery 20с — ХОТ-ПУТЬ отправки, зависший RAG больше не морозит send;
    ragIngest 30с), init.ts (fetchInit 15с — старт), mcp.ts (listMcp 15с / addMcp 30с / removeMcp 15с).
  • Теперь НИ ОДИН клиентский fetch не может висеть вечно при зависшем бэкенде. Проверено: tsc=0, перезагрузка —
    pageOk:true, композер на месте, /api/init 200 (курс ₽72.4 живой). Happy-path цел, поведение при успехе не изменилось.

- ~13:25 — ✅ HARDEN-4 (edge-cases оркестратора + half-wire команды):
  • ОТМЕНА НА ПОЛПУТИ: оркестратор гонял до 120с без возможности прервать. Добавил кнопку «стоп» (реальный AbortController,
    проброс signal в lib/orchestrate) + закрытие панели тоже отменяет прогон (не висит впустую). Повторный запуск отменяет предыдущий.
  • ПУСТЫЕ СОСТОЯНИЯ: панель без задачи показывала пустоту → подсказка «введи задачу…»; успех с пустым результатом → «ничего не вернулось, переформулируй».
  • HALF-WIRE: бара `/orchestrate` (без задачи) была no-op (`if(arg)`) → теперь открывает пустую панель (как «+»-меню).
  • Проверено вживую (браузер): /orchestrate → панель+подсказка; запустил задачу → спиннер+кнопка «стоп»; клик «стоп» → спиннер
    исчез, показано «остановлено». panelClosed/composerCleared ок. tsc=0.

- ~13:55 — ✅ HARDEN-5 (фидбэк headline-фичи: супер-поиск больше не «висит молча»):
  • БАГ UX: runSuperSearch ждал до 75с ДО send() — всё это время только тост (гаснет за 2.6с), юзер видел пустоту и думал «завис».
    Добавил постоянный индикатор «Ищу в сети — собираю источники…» (спиннер) внутри composer на всё время поиска.
  • Блок двойной отправки во время поиска (`searchingWeb` в submit-гарде + в самом runSuperSearch) — не плодим параллельные поиски.
  • Индикатор в общем `composer` → виден и на велком-экране, и в активном чате. try/finally гарантирует снятие даже при ошибке/таймауте.
  • Проверено вживую: /search → баннер появился и держался весь поиск (~10с) → снялся по завершении → отрисовался ответ + панель «Источники».
    Двойная отправка заблокирована. tsc=0. Тестовые чаты подчищены (22→20).

- ~14:15 — ✅ HARDEN-6 (отмена супер-поиска — паттерн «стоп» теперь везде одинаковый):
  • Супер-поиск (до 75с) нельзя было прервать — только ждать. Добавил кнопку «стоп» прямо в индикатор поиска
    (проброс AbortSignal в lib/superSearch). При отмене НЕ отправляем запрос дальше — просто выходим + тост «Поиск остановлен».
  • Теперь отмена единообразна: оркестратор и супер-поиск оба прерываются кнопкой «стоп».
  • Проверено вживую: /search → баннер+«стоп» → клик → баннер снялся, тост «Поиск остановлен», ответ/источники НЕ добавились
    (прервано до send). Чатов 20 (тест-чат не утёк). tsc=0.

- ~14:35 — ✅ ПРОВЕРКА КАЧЕСТВА headline-фичи (супер-поиск, не код — верификация вживую):
  • Инлайн-цитаты [N]: рендерятся кликабельными циан-чипами с якорями #msgId-src-N (запрос про OpenAI → 9 цитат). citeify работает.
  • Скролл по клику цитаты: scrollIntoView к источнику РАБОТАЕТ (проверил: 969px → 169px, источник в зоне видимости). Не баг.
  • Панель «Источники» всегда рендерит все источники с якорями (11 шт.) — кликабельны независимо от инлайн-[N].
  • НАХОДКА (поведение модели, не код): модель НЕ всегда ставит инлайн-[N] (OpenAI→9, Tesla→0). Панель источников — надёжный
    фолбэк, источники всегда на месте. Авто-простановку [N] НЕ делаю — рискует приписать факт не тому источнику (хуже, чем без неё).
  • Консоль чистая (0 ошибок) после всех прогонов. Тестовые чаты подчищены.

- ~14:50 — ✅ CO-BROWSE проверен вживую (после моего изменения таймаута): /browse example.com → панель открылась,
  ЖИВОЙ снимок Playwright 1280×800 отрисовался за ~2с, url = https://example.com/, ошибок нет. Закрытие панели освободило
  сессию (browserClose). Таймаут не сломал happy-path. ₽/баланс проверен ранее (/api/init 200, курс 72.4). Все 4 дифференциатора
  проверены ЭТОЙ сессией.

- ~15:05 — ✅ HARDEN-7 (отмена в браузере — паттерн «стоп» теперь во ВСЕХ трёх async-фичах):
  • Добавил «стоп» в браузер-панель (проброс AbortSignal в lib/browser call() + все действия open/click/link/back/scroll).
    Кнопка появляется поверх снимка во время загрузки; клик прерывает действие, панель остаётся открытой (не рвём сессию).
    Повторное действие отменяет предыдущее.
  • Проверено вживую: /browse тяжёлой страницы → «стоп» во время загрузки → «остановлено», панель открыта. tsc=0, сессия освобождена.
  • ИТОГ по отмене: супер-поиск + оркестратор + браузер — все три прерываются единообразно кнопкой «стоп».

- ~15:20 — ✅ РЕГРЕССИЯ обычной отправки (я менял submit-гард + send-путь): отправил простое сообщение «столица Японии»
  → композер очистился, ответ «Токио» стримился нормально. Мои правки (searchingWeb-гард, индикатор) НЕ сломали обычный чат. Тест-чат убран.

- ~15:45 — ✅ ОРКЕСТРАТОР: ЖИВОЙ СТРИМИНГ (Дамир: «too long» — упирался в таймаут, спиннер пустой 90с):
  • ПРИЧИНА: фронт делал ОДИН буферизованный запрос → ждал весь прогон (60-90с, воркеры ищут в вебе) → таймаут, ноль фидбэка.
  • Бэкенд УЖЕ умел SSE (stream:true, emit_sse: step/agent→done) — не использовали. Завёл фронт-стриминг:
    - прокси /api/orchestrate прокидывает text/event-stream как есть (не буферит .json)
    - lib/orchestrateStream(): читает SSE, парсит data:, каждый шаг → onStep (план/агенты сразу)
    - панель: live-steps в state, план/агенты рисуются по мере прилёта, синтез в конце
  • Проверено: curl SSE — план за ~4с, агенты стримятся, done с final (4 step+5 agent+done). Браузер — план появился за ~2с,
    агенты live во время прогона (поймал дважды). 0 ошибок консоли, tsc=0.
  • РЕЗУЛЬТАТ: вместо 90с пустого спиннера→таймаут — план за пару секунд + агенты работают на глазах. Бэкенд всё равно
    небыстрый (воркеры ищут в вебе) — это лана ural (можно ускорить меньшим числом воркеров/без веба), но фидбэк теперь живой.

- ~16:30 — ✅ CO-BROWSE «ОТ ЮЗЕРА» — РАСШИРЕНИЕ (Дамир выбрал extension; цель: твой IP/куки → нет капчи, ИИ видит):
  • КЛЮЧ: бэкенд НЕ нужен. Расширение и страница Тайги в одном браузере → связь через window.postMessage. ИИ видит кадры
    обычным путём Тайги. Весь co-browse-через-расширение целиком в лане CORE, server.py не трогал (он залочен amur).
  • Расширение MV3 (новая папка taiga-web/cobrowse-extension/, 6 файлов): manifest + content.js (мост на стр.Тайги /
    агент на целевой: клик по координатам/скролл/ввод + scrape текста+ссылок) + background.js (целевой сайт в ОТДЕЛЬНОМ окне
    → твой IP/куки; captureVisibleTab → кадр; роутинг) + popup (настройка origin) + README (протокол).
  • Веб-сторона (лана CORE): lib/cobrowse-ext.ts (детект/подписка/действия по postMessage) + browser-panel: тумблер
    «с моего ПК» (виден только если расширение стоит), ext-режим роутит open/click/scroll/back/ссылки через расширение,
    подписка на кадры рисует снимок; куки-поле прячется (в ext-режиме куки твои автоматом).
  • Проверено: tsc=0; без расширения деградирует чисто (тумблер скрыт, серверный режим цел, панель рендерится, 0 рантайм-ошибок).
    Полный цикл расширения (загрузить в Chrome) проверяет Дамир вживую — в headless-превью расширения нет.

- ~17:00 — FINISH-PLAN S1: продолжаю CORE-A. Замок на chat.tsx взял (kedr).
- ~17:20 — ✅ РЕЖИМЫ Fast/Auto/Expert/Heavy (Grok) — components/effort-selector.tsx + ось `effort` в chat.tsx (персист).
  Маппинг: Fast→fastChatChain+коротко (без памяти/связок), Auto→авто-роутер (дефолт), Expert→deep-пайплайн (разбор по шагам),
  Heavy→brain (дешёвая-контролёр→сильная). Селектор в баре композера. Проверено вживую: Fast→«Париж» быстро,
  Expert→виден многошаговый процесс, Heavy→полный ответ без ошибок. tsc=0.

- ~17:40 — ✅ СЛАЙДЕР «ПАМЯТЬ: ПОСЛЕДНИЕ N СООБЩЕНИЙ» (RedPill): components/memory-window.tsx (чип «контекст: N/всё» +
  поповер-слайдер 0..40) + ось memWindow в chat.tsx (персист) + historyCap в use-taiga-chat (режет wireHistory до N,
  сводку «〔Сводка ранее〕» сохраняет). Прокинул в fast/relay-brain/обычный/тривиальный send.
  Проверено вживую через fetch-перехват: при контекст=2 в /api/chat реально уходит 2 сообщения (не 3+), хотя в UI все. tsc=0.

- ~18:05 — ✅ БАГ ФОН-РАДУГА (со скринов): фон студии «протекал» сквозь панели. components/mode-background.tsx —
  добавил скрим-маску: рабочие режимы (image/code, плотный UI) → РАВНОМЕРНЫЙ сильный скрим rgba(0,0,0,0.96)
  (радуга не лезет ни в центре, ни по КРАЯМ); showcase (чат/ультра) → лёгкая виньетка (фон виден, но читаемо).
  Итерировал по скринам: 0.78-радиал (края текли) → 0.88 (слабый след) → 0.96 равномерный = ЧИСТО.
  Проверено скринами: студия на чистом тёмном фоне, UI читаем; чат сохранил ауру. tsc=0.
- ~18:05 — ✅ P0 СТУДИЯ (verify): переключил на Видео → форс-ре-рендер (тумблер сайдбара) → студия ОСТАЛАСЬ на Видео,
  картинка тигра сгенерилась. Сброса в «Картинки» нет. (фикс keep-alive+memo держится.)

- ~18:25 — ✅ TOOLS-МЕНЮ как ChatGPT: в «+»-меню (plusItems) добавил недостающие тулзы-ChatGPT с тумблерами:
  Поиск по файлам·база знаний (RAG)→KnowledgePanel · Навыки·библиотека→skill-builder · Запуск кода·песочница(dev)→devMode ·
  Артефакты·Канвас→openCanvas(последний артефакт). Веб-поиск/Супер-поиск уже были.
  Проверено вживую: «+» показывает File Search/Skills/Run Code/Artifacts/Web Search; клик «Поиск по файлам»→панель знаний;
  «Запуск кода»→devMode тумблер+тост (персист). tsc=0.

### ✅✅ S1 CORE-A (FINISH-PLAN) DONE — все 5 задач блока зелёные:
  1. P0-сброс студии — устойчив (проверено: Видео остаётся после ре-рендера) ✅
  2. Tools-меню ChatGPT (File Search/Web/Skills/Run Code/Artifacts, тумблеры) ✅
  3. Режимы Fast/Auto/Expert/Heavy (Grok) — проверены вживую ✅
  4. Слайдер «память: N сообщений» — реально режет запрос (fetch-перехват) ✅
  5. Баг фон-радуга — равномерный скрим, студия на чистом фоне (скрины) ✅
  tsc=0 всю дорогу, замок на chat.tsx держу, файлы Studio/server.py не трогал. Беру дальше из беклога/🅝nice.

- ~18:45 — ✅ МОБАЙЛ-ПРОХОД (DoD must, беклог): проверил чат-поверхность на 375×812.
  Нет горизонтального оверфлоу (scrollWidth=vw=375) · тулбар композера flex-wrap (Fast/Auto/Expert/Heavy + чипы переносятся,
  не режутся) · кнопка «Отправить» в зоне видимости · сообщение шлётся и отвечает · боковые панели (оркестратор/браузер)
  на мобайле во ВСЮ ширину (w-full=375, не узкий десктоп-сайдбар). Поломок нет — поверхность адаптивна. Тест-чат убран.

- ~19:10 — ✅✅ /PIPELINE — ЮЗЕР-КОНСТРУКТОР ЦЕПОЧЕК модель→модель→модель (ГЛАВНАЯ фишка Damir, 🅜):
  lib/pipelines.ts (тип Pipeline/Stage, 3 встроенных: Черновик→Полировка/Кодер→Ревью/Разбор→Тезисы, save/load/del,
  materializeStages: {{input}}→запрос) + components/pipeline-builder.tsx (визуальный билдер: шаги с пикером модели на КАЖДЫЙ
  шаг + промпт-шаблон {{prev}}/{{input}}, добавить/удалить/переставить шаг, сохранить свой, поле запроса, «Запустить»).
  Wired: команда /pipeline (builtins) + runPipeline→sendPipeline (бэкенд протягивает {{prev}}). НЕ конфликтует с «duo» (fixed).
  Проверено вживую: /pipeline→билдер со встроенными → ввёл «слоган для VPN» → Запустить → Шаг1 (черновик) → Шаг2 (полировка
  через {{prev}}) → финальный слоган. model→model реально протянулся (скрин). tsc=0.

- ~19:30 — ✅ /UNCENSOR команда (CORE, бэкенд rewrite_uncensored есть у ural): прокси src/app/api/uncensor/route.ts +
  lib/uncensor.ts (uncensorRewrite с таймаутом/фолбэком) + переделал команду из kind:prompt в handler «uncensor»
  (chat.tsx): /uncensor <q> → переписывает запрос под прямой ответ → СРАЗУ отвечает в uncensored-режиме (один шаг,
  а не «верни переписанный запрос»). Проверено: /uncensor про реверс APK → реальный технический ответ (JADX/jadx-gui,
  декомпиляция Java) без морали. curl бэкенда 200. tsc=0.

- ~19:50 — ✅ ТОГГЛ-ЛИМИТ ТРАТ НА ОПЕРАЦИЮ (🅝, «stop after $X» как в Claude Code): components/spend-cap.tsx
  (чип «лимит: $X/без» + поповер: без лимита/$0.05/$0.20/$1, ₽-эквивалент) + ось spendCap в chat.tsx (персист) +
  gateSpend(estUsd,label,run): операция дороже лимита → модалка «Дороже лимита: ≈$X, лимит $Y, Продолжить?» (Отмена/Потратить).
  Завёл гейт на конвейер (≈$0.012/шаг) + проброс max_spend во все chat-запросы (бэкенд-хук hard-stop — enforcement у ural).
  Проверено вживую: лимит $0.05 + 5-шаговый конвейер ($0.06) → модалка появилась; «Отмена» РЕАЛЬНО заблокировала запуск;
  max_spend=0.05 доходит до /api/chat (fetch-перехват). tsc=0.

### ✅✅✅ ADD-ON CORE (kedr) — все 3 задачи зелёные:
  1. /pipeline — юзер-конструктор цепочек модель→модель→модель (🅜 ГЛАВНАЯ фишка) — проверено end-to-end ✅
  2. /uncensor — переписчик→прямой ответ в один шаг (🅜) — проверено (реверс APK→JADX) ✅
  3. Тоггл-лимит трат на операцию (🅝) — гейт+модалка+max_spend — проверено ✅

- ~20:15 — ✅ P0-РЕМАУНТ студии (ADDON-FINAL, amur флагал): подтвердил CORE-сторону пуленепробиваемой —
  студия ВСЕГДА смонтирована (CSS-hidden вне image, НЕ unmount) + memo(ImageStudio) + showToast стабилен (useCallback []) →
  memo реально работает, студия НЕ ре-рендерится на токенах чата. Добавил key="studio-keepalive" (гарантия от ремаунта при
  смене соседей в дереве). Проверено вживую: Видео → переключил режим на Чат → вернулся на Картинки → СТУДИЯ ОСТАЛАСЬ НА ВИДЕО
  (state не слетел). Если amur всё ещё видит сброс — это внутренний таб-state image-studio.tsx (его лана), CORE-mount железобетон.
- ~20:20 — ✅ 3 ЯРУСА Обычный/Студия/Ультра (реструктур верх-нав): components/tier-nav.tsx (3 крупных входа с подписями) +
  заменил ModePills на TierNav (welcome+студия). pickTier: Обычный→chat, Студия→image, Ультра→ultra+effort heavy+noRefusal.
  Fast/Auto/Expert/Heavy остаётся осью усилия ВНУТРИ яруса. Ультра = топ-модель + глубоко + без отказов (composeSystem
  добавляет MODE_SYS.uncensored при noRefusal). Проверено вживую: 3 яруса рендерятся; Ультра→тост+effort heavy; Студия→студия;
  Обычный→чат; и КЛЮЧЕВОЕ — перехват системного промпта в Ультре содержит И «глубокое мышление» И «без цензуры/морализаторства». tsc=0.

- ~20:35 — ✅ ХУК ТЕМ + VOICE для sosna (разблокировка CORE-B): lib/theme.ts (реестр THEMES 4 шт + applyTheme: data-theme +
  CSS-переменные акцента + персист + initTheme на маунте chat.tsx) + lib/use-voice-input.ts (переиспользуемый STT-хук, тот же
  Web Speech движок). + HOOK-FOR-SOSNA.md (точки инъекции, что её/что моё). Проверено: на старте data-theme=midnight,
  --taiga-accent=#22d3ee; смена на cyberpunk → #f0abfc, переживает перезагрузку. Voice — извлечён из рабочего композера. tsc=0.

### ✅✅✅ ADDON-FINAL CORE (kedr) — ВСЕ 7 задач зелёные:
  1. P0-ремаунт студии — CORE-mount пуленепробиваем (always-mount+memo+stable key+verified persist) ✅
  2. /pipeline — юзер-конструктор цепочек (главная фишка) ✅  3. 3 яруса Обычный/Студия/Ультра ✅
  4. /uncensor ✅  5. лимит трат ✅  6. «+»-tools-меню (File Search/Run Code/Skills/Artifacts) ✅
  7. хук тем+Voice для sosna ✅
  Консоль 0 ошибок, tsc=0 всю дорогу, замок на chat.tsx, server.py/studio не трогал (mcp.ts — ural).

## Итог сессии kedr (CORE) — закрыто 12 + harden×7 + QA + регрессия + стриминг-оркестратор
Все дифференциаторы из focus-листа доведены до «за это платят» + market-fit (₽) + организация (папки/пресеты) +
расширяемость (MCP). Робастность: таймауты на ВСЕХ fetch (ни один не виснет), отмена «стоп» во ВСЕХ трёх async-фичах
(супер-поиск/оркестратор/браузер), пустые/ошибочные состояния, индикатор поиска. Все 4 дифференциатора + обычная отправка
проверены вживую ЭТОЙ сессией (citations+скролл, оркестратор-отмена, co-browse-снимок, ₽). Консоль 0 ошибок.
tsc=0 всю дорогу, файлы Studio/ural не тронуты, тестовые данные подчищены.
